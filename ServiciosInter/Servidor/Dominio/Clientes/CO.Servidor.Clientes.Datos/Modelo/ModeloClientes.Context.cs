//------------------------------------------------------------------------------
// <auto-generated>
//    This code was generated from a template.
//
//    Manual changes to this file may cause unexpected behavior in your application.
//    Manual changes to this file will be overwritten if the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------

namespace CO.Servidor.Clientes.Datos.Modelo
{
    using System;
    using System.Collections.Generic;
    using System.Data;
    using System.Data.Entity;
    using System.Data.Entity.Infrastructure;
    using System.Linq.Expressions;
    using System.Linq;
    using System.Reflection;
    using System.Data.Objects;
    using System.Globalization;
    
    
    public partial class EntidadesClientes : DbContext
    {
        public EntidadesClientes(string ConnectionString)
            : base(ConnectionString)
        {
    				
        }
    
        protected override void OnModelCreating(DbModelBuilder modelBuilder)
        {
            throw new UnintentionalCodeFirstException();
        }
    		#region Metodos para filtrar
    
        /// <summary>
        /// Crea una expresion de tipo where equals para todos los filtros ingresados
        /// </summary>
        /// <typeparam name="TLinqEntity">Entidad sobre la cual se creará la expresion</typeparam>
        /// <param name="filtros">Filtros que se agregaran a la expresión equals</param>
        /// <returns>Expresion</returns>
        public Expression<Func<TLinqEntity, bool>> ObtenerExpresionEquals<TLinqEntity>(IDictionary<string, string> filtros)
        {
          List<Expression<Func<TLinqEntity, bool>>> lexList = new List<Expression<Func<TLinqEntity, bool>>>();
    
          Expression<Func<TLinqEntity, bool>> lamExp = ArmarExpresionFiltrado<TLinqEntity>(filtros, "Equals");
          if (lamExp != null)
            lexList.Add(lamExp);
    
          if (lexList.Count == 0)
          {
            Expression<Func<TLinqEntity, bool>> lambda = x => 1 == 1;
            return lambda;
          }
          else
            return ((Expression<Func<TLinqEntity, bool>>)lexList[lexList.Count - 1]);
        }
    
        /// <summary>
        /// Crea una expresion de tipo where contains para todos los filtros ingresados
        /// </summary>
        /// <typeparam name="TLinqEntity">Entidad sobre la cual se creará la expresion</typeparam>
        /// <param name="filtros">Filtros que se agregaran a la expresión contains</param>
        /// <returns>Expresion</returns>
        public Expression<Func<TLinqEntity, bool>> ObtenerExpresionContains<TLinqEntity>(IDictionary<string, string> filtros)
        {
          List<LambdaExpression> lexList = new List<LambdaExpression>();
    
          LambdaExpression lamExp = ArmarExpresionFiltrado<TLinqEntity>(filtros, "Contains");
          if (lamExp != null)
            lexList.Add(lamExp);
    
          if (lexList.Count == 0)
          {
            Expression<Func<TLinqEntity, bool>> lambda = x => 1 == 1;
            return lambda;
          }
          else
            return ((Expression<Func<TLinqEntity, bool>>)lexList[lexList.Count - 1]);
        }
    
        /// <summary>
        /// Crea una expresion de tipo where para todos los filtros y expresiones where ingresadas
        /// </summary>
        /// <typeparam name="TLinqEntity">Entidad sobre la cual se creará la expresion</typeparam>
        /// <param name="filtros">Filtros que se agregaran a la expresión contains</param>
        /// <param name="where">Diccionario con la lista de expresiones where que se desean concatenar con los filtros</param>
        /// <returns>Expresion</returns>
        public Expression<Func<TLinqEntity, bool>> ObtenerFiltro<TLinqEntity>(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where)
        {
          List<Expression<Func<TLinqEntity, bool>>> lexList = new List<Expression<Func<TLinqEntity, bool>>>();
    
          Expression<Func<TLinqEntity, bool>> lamExp = ArmarExpresionFiltrado<TLinqEntity>(filtros, "Contains");
          if (lamExp != null)
            lexList.Add(lamExp);
    
          foreach (KeyValuePair<LambdaExpression, OperadorLogico> expr in where)
          {
            if (lexList.Count == 0)
              lexList.Add((Expression<Func<TLinqEntity, bool>>)expr.Key);
            else
            {
              Expression<Func<TLinqEntity, bool>> bodyOuter = null;
    
              switch (expr.Value)
              {
                case OperadorLogico.And:
    
                  bodyOuter = ((Expression<Func<TLinqEntity, bool>>)expr.Key).And((Expression<Func<TLinqEntity, bool>>)lexList[lexList.Count - 1]);
                  break;
                case OperadorLogico.Or:
                  bodyOuter = ((Expression<Func<TLinqEntity, bool>>)expr.Key).Or((Expression<Func<TLinqEntity, bool>>)lexList[lexList.Count - 1]);
                  break;
              }
    
              lexList.Add(bodyOuter);
            }
          }
    
          if (lexList.Count == 0)
          {
            Expression<Func<TLinqEntity, bool>> lambda = x => 1 == 1;
            return lambda;
          }
    
          return (lexList[lexList.Count - 1]);
        }
    
        /// <summary>
        /// Crea una expresion lambda de tipo where a partir de los parámetros ingresados
        /// </summary>
        /// <typeparam name="TLinqEntity">Entidad sobre la cual se creará la expresion</typeparam>
        /// <param name="nombreCampo">Nombre del campo de la entidad sobre el cual se aplicará el operador de comparación</param>
        /// <param name="valor">Valor con el cual se comparará el campo de la entidad</param>
        /// <param name="operComparacion">Operacion de comparacion de la expresion</param>
        /// <param name="valor2">Valor utilizado en las expresiones cuyo operador de comparacion es el between</param>
        /// <returns>Expresion Lambda</returns>
        public LambdaExpression CrearExpresionLambda<TLinqEntity>(string nombreCampo, string valor, OperadorComparacion operComparacion, string valor2 = null)
        {
          LambdaExpression expresionLambda = null;
          ParameterExpression param = Expression.Parameter(typeof(TLinqEntity), nombreCampo);
    
          bool valido = false;
          PropertyInfo prop = typeof(TLinqEntity).GetProperty(nombreCampo);
          Type proptype = prop.PropertyType;
    
          Expression callExpr = Expression.MakeMemberAccess(param, prop);
          Expression valueExpr = Expression.Constant(valor);
          Expression body = null;
    
          if (proptype == typeof(string))
          {
            var toLower = Expression.Call(callExpr, typeof(string).GetMethod("ToLower", System.Type.EmptyTypes));
            //body = Expression.Call(toLower, typeof(string).GetMethod("Contains", new Type[] { typeof(string) }), valueExpr);
            valido = true;
          }
    
          else if (proptype == typeof(int) || proptype == typeof(Nullable) ||
              proptype == typeof(long) ||
              proptype == typeof(short) ||
              proptype == typeof(decimal) ||
              proptype == typeof(System.Decimal) ||
              proptype == typeof(System.Int16) ||
              proptype == typeof(System.Int32) ||
              proptype == typeof(System.Int64) ||
              proptype == typeof(System.Nullable<decimal>) ||
              proptype == typeof(System.Nullable<long>) ||
              proptype == typeof(System.Nullable<int>) ||
              proptype == typeof(System.Nullable<short>))
          {
            // Check if input data type matches
            object num = null;
    
            if (proptype == typeof(int) || proptype == typeof(System.Int32) || proptype == typeof(System.Nullable<int>))
            {
              int temp;
              if (int.TryParse(valor, out temp))
                num = temp;
            }
    
    				if (proptype == typeof(decimal) || proptype == typeof(System.Decimal) || proptype == typeof(System.Nullable<decimal>))
            {
              decimal temp;
              if (decimal.TryParse(valor, out temp))
                num = temp;
            }
    
            if (proptype == typeof(short) || proptype == typeof(System.Int16) || proptype == typeof(System.Nullable<short>))
            {
              short temp;
              if (short.TryParse(valor, out temp))
                num = temp;
            }
    
            if (proptype == typeof(long) || proptype == typeof(System.Int64) || proptype == typeof(System.Nullable<long>))
            {
              long temp;
              if (long.TryParse(valor, out temp))
                num = temp;
            }
    
            if (num != null)
            {
              valueExpr = Expression.Constant(num, proptype);
              body = Expression.Equal(callExpr, valueExpr);
              valido = true;
            }
          }
          else if (proptype == typeof(DateTime) || proptype == typeof(Nullable))
          {
            DateTime date;
            CultureInfo cultura = new CultureInfo("es-CO");
            if (DateTime.TryParse(valor,  cultura, DateTimeStyles.None, out date))
            {
    			 if (valor2 == null)
                valor2 = date.AddDays(1).ToString("yyyy/MM/dd");
              valueExpr = Expression.Constant(date, proptype);
              //Expression valueExpr2 = Expression.Constant(date.AddHours(24), proptype);
              //Expression bodyBetween = Expression.GreaterThanOrEqual(callExpr, valueExpr);
              //Expression body1 = Expression.LessThan(callExpr, valueExpr2);
              //body = Expression.AndAlso(bodyBetween, body1);
              valido = true;
            }
          }
          else if (proptype == typeof(bool) || proptype == typeof(System.Nullable<bool>) || proptype == typeof(Nullable))
          {
            bool val;
            if (bool.TryParse(valor, out val))
            {
              valueExpr = Expression.Constant(val, proptype);
              //body = Expression.Equal(callExpr, valueExpr);
              valido = true;
            }
          }
    
          if (valido)
          {
            switch (operComparacion)
            {
              case OperadorComparacion.Equal:
                body = Expression.Equal(callExpr, valueExpr);
                break;
              case OperadorComparacion.NotEqual:
                body = Expression.NotEqual(callExpr, valueExpr);
                break;
              case OperadorComparacion.Contains:
                if (proptype == typeof(string))
                {
                  var toLower = Expression.Call(callExpr, typeof(string).GetMethod("ToLower", System.Type.EmptyTypes));
                  body = Expression.Call(toLower, typeof(string).GetMethod("Contains", new Type[] { typeof(string) }), valueExpr);
                }
                else
                {
                  throw new Exception("El operador de comparación 'Contains' solo soporta campos de tipo de dato string");
                }
                break;
              case OperadorComparacion.Between:
                if (proptype == typeof(DateTime))
                {
                  DateTime date;
                  if (DateTime.TryParse(valor2, out date))
                  {
                    Expression valueExpr2 = Expression.Constant(date, proptype);
                    Expression bodyBetween = Expression.GreaterThanOrEqual(callExpr, valueExpr);
                    Expression body1 = Expression.LessThan(callExpr, valueExpr2);
                    body = Expression.AndAlso(bodyBetween, body1);
                  }
                  else
                  {
                    throw new Exception("El formato de Fecha para el segundo valor no es válido");
                  }
                }
                else
                {
                  throw new Exception("El operador de comparación 'Between' solo soporta campos de tipo de dato datetime");
                }
                break;
            }
    
            expresionLambda = Expression.Lambda(body, param);
          }
    
          if (expresionLambda == null)
            throw new Exception("Argumentos no válidos para construir la expresión lambda");
    
          return expresionLambda;
        }
    
        private Expression<Func<TLinqEntity, bool>> ArmarExpresionFiltrado<TLinqEntity>(IDictionary<string, string> filtros, string tipoFiltro)
        {
          List<Expression<Func<TLinqEntity, bool>>> lexList = new List<Expression<Func<TLinqEntity, bool>>>();
          foreach (KeyValuePair<string, string> filtro in filtros)
          {
            OperadorComparacion opercomp = OperadorComparacion.Contains;
            PropertyInfo prop = typeof(TLinqEntity).GetProperty(filtro.Key);
            Type proptype = prop.PropertyType;
            if (proptype == typeof(string))
            {
              switch (tipoFiltro)
              {
                case "Equals":
                  opercomp = OperadorComparacion.Equal;
                  break;
                default:
                  opercomp = OperadorComparacion.Contains;
                  break;
              }
            }
            else if (proptype == typeof(int) ||
                   proptype == typeof(long) ||
                   proptype == typeof(short) ||
                   proptype == typeof(decimal) ||
                   proptype == typeof(System.Int16) ||
                   proptype == typeof(System.Int32) ||
                   proptype == typeof(System.Int64) ||
                   proptype == typeof(System.Decimal) ||
                   proptype == typeof(System.Nullable<long>) ||
                   proptype == typeof(System.Nullable<int>) ||
                   proptype == typeof(System.Nullable<short>) ||
                   proptype == typeof(System.Nullable<decimal>) ||
                   proptype == typeof(bool) ||
                   proptype == typeof(System.Nullable<bool>))
            {
              opercomp = OperadorComparacion.Equal;
            }
            else if (proptype == typeof(DateTime) || proptype == typeof(System.Nullable<DateTime>))
            {
              opercomp = OperadorComparacion.Between;
            }
    
            Expression<Func<TLinqEntity, bool>> operLambda = (Expression<Func<TLinqEntity, bool>>)CrearExpresionLambda<TLinqEntity>(filtro.Key, filtro.Value, opercomp);
            if (lexList.Count == 0)
              lexList.Add(operLambda);
            else
            {
              Expression<Func<TLinqEntity, bool>> bodyOuter = null;
              bodyOuter = (operLambda).And(lexList[lexList.Count - 1]);
    
              lexList.Add(bodyOuter);
            }
          }
    
          if (lexList.Count > 0)
            return lexList[lexList.Count - 1];
    
          //List<ParameterExpression> paramList = new List<ParameterExpression>();
          //foreach (KeyValuePair<string, string> filtro in filtros)
          //{
          //  paramList.Add(Expression.Parameter(typeof(TLinqEntity), filtro.Key));
          //}
    
          //List<Expression<Func<TLinqEntity, bool>>> lexList = new List<Expression<Func<TLinqEntity, bool>>>();
          //for (int i = 0; i < filtros.Count; i++)
          //{
          //  bool valido = false;
          //  PropertyInfo prop = typeof(TLinqEntity).GetProperty(filtros.ElementAt(i).Key);
          //  Type proptype = prop.PropertyType;
    
          //  Expression callExpr = Expression.MakeMemberAccess(paramList[i], prop);
          //  Expression valueExpr = Expression.Constant(filtros.ElementAt(i).Value.ToLower());
          //  Expression body = null;
    
          //  if (proptype == typeof(string))
          //  {
          //    var toLower = Expression.Call(callExpr, typeof(string).GetMethod("ToLower", System.Type.EmptyTypes));
          //    if (tipoFiltro.Equals("Contains"))
          //      body = Expression.Call(toLower, typeof(string).GetMethod("Contains", new Type[] { typeof(string) }), valueExpr);
          //    else if (tipoFiltro.Equals("Equals"))
          //      body = Expression.Call(toLower, typeof(string).GetMethod("Equals", new Type[] { typeof(string) }), valueExpr);
          //    valido = true;
          //  }
    
          //  else if (proptype == typeof(int) || proptype == typeof(Nullable) ||
          //          proptype == typeof(long) ||
          //          proptype == typeof(short) ||
          //          proptype == typeof(System.Int16) ||
          //          proptype == typeof(System.Int32) ||
          //          proptype == typeof(System.Int64) ||
          //          proptype == typeof(System.Nullable<long>) ||
          //          proptype == typeof(System.Nullable<int>) ||
          //          proptype == typeof(System.Nullable<short>))
          //  {
          //    // Check if input data type matches
          //    object num = null;
    
          //    if (proptype == typeof(int) || proptype == typeof(System.Int32) || proptype == typeof(System.Nullable<int>))
          //    {
          //      int temp;
          //      if (int.TryParse(filtros.ElementAt(i).Value, out temp))
          //        num = temp;
          //    }
    
          //    if (proptype == typeof(short) || proptype == typeof(System.Int16) || proptype == typeof(System.Nullable<short>))
          //    {
          //      short temp;
          //      if (short.TryParse(filtros.ElementAt(i).Value, out temp))
          //        num = temp;
          //    }
    
          //    if (proptype == typeof(long) || proptype == typeof(System.Int64) || proptype == typeof(System.Nullable<long>))
          //    {
          //      long temp;
          //      if (long.TryParse(filtros.ElementAt(i).Value, out temp))
          //        num = temp;
          //    }
    
          //    if (num != null)
          //    {
          //      valueExpr = Expression.Constant(num, proptype);
          //      body = Expression.Equal(callExpr, valueExpr);
          //      valido = true;
          //    }
          //  }
          //  else if (proptype == typeof(DateTime) || proptype == typeof(Nullable))
          //  {
          //    DateTime date;
          //    if (DateTime.TryParse(filtros.ElementAt(i).Value, out date))
          //    {
          //      valueExpr = Expression.Constant(date, proptype);
          //      Expression valueExpr2 = Expression.Constant(date.AddHours(24), proptype);
          //      Expression bodyBetween = Expression.GreaterThanOrEqual(callExpr, valueExpr);
          //      Expression body1 = Expression.LessThan(callExpr, valueExpr2);
          //      body = Expression.AndAlso(bodyBetween, body1);
          //      valido = true;
          //    }
          //  }
          //  else if (proptype == typeof(bool) || proptype == typeof(Nullable))
          //  {
          //    bool val;
          //    if (bool.TryParse(filtros.ElementAt(i).Value, out val))
          //    {
          //      valueExpr = Expression.Constant(val, proptype);
          //      body = Expression.Equal(callExpr, valueExpr);
          //      valido = true;
          //    }
          //  }
    
          //  if (valido)
          //  {
          //    if (lexList.Count == 0)
          //    {
          //      lexList.Add((Expression<Func<TLinqEntity, bool>>)body);
          //    }
          //    else
          //    {
          //      Expression<Func<TLinqEntity, bool>> bodyOuter = null;
    
          //      bodyOuter = ((Expression<Func<TLinqEntity, bool>>)body).And((Expression<Func<TLinqEntity, bool>>)lexList[i - 1]);
    
          //      //Expression bodyOuter = Expression.AndAlso(
          //      //                    body,
          //      //                    Expression.Invoke(lexList[i - 1], paramList[i]));
          //      lexList.Add(bodyOuter);
          //    }
          //  }
          //}
    
          //if (lexList.Count > 0)
          //  return lexList[lexList.Count - 1];
          return null;
        }
    
        #endregion Metodos para filtrar
    		
    		    #region Consultas
    	/// <summary>
    		/// Retorna la lista de TipoActividadEconomica_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<TipoActividadEconomica_PAR> ConsultarContainsTipoActividadEconomica_PAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<TipoActividadEconomica_PAR, bool>> expresionFiltrado = ObtenerExpresionContains<TipoActividadEconomica_PAR>(filtro);
    			return ConsultarTipoActividadEconomica_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de TipoActividadEconomica_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<TipoActividadEconomica_PAR> ConsultarEqualsTipoActividadEconomica_PAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<TipoActividadEconomica_PAR, bool>> expresionFiltrado = ObtenerExpresionEquals<TipoActividadEconomica_PAR>(filtro);
    				return ConsultarTipoActividadEconomica_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de TipoActividadEconomica_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<TipoActividadEconomica_PAR> ConsultarTipoActividadEconomica_PAR(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<TipoActividadEconomica_PAR, bool>> expresionFiltrado = ObtenerFiltro<TipoActividadEconomica_PAR>(filtros,where);
    				return ConsultarTipoActividadEconomica_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de TipoActividadEconomica_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<TipoActividadEconomica_PAR> ConsultarTipoActividadEconomica_PAR(Expression<Func<TipoActividadEconomica_PAR, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<TipoActividadEconomica_PAR>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de TipoActividadEconomica_PAR que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<TipoActividadEconomica_PAR> ConsultarTipoActividadEconomica_PAR(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<TipoActividadEconomica_PAR>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de TipoRegimenContributivo_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<TipoRegimenContributivo_PAR> ConsultarContainsTipoRegimenContributivo_PAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<TipoRegimenContributivo_PAR, bool>> expresionFiltrado = ObtenerExpresionContains<TipoRegimenContributivo_PAR>(filtro);
    			return ConsultarTipoRegimenContributivo_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de TipoRegimenContributivo_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<TipoRegimenContributivo_PAR> ConsultarEqualsTipoRegimenContributivo_PAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<TipoRegimenContributivo_PAR, bool>> expresionFiltrado = ObtenerExpresionEquals<TipoRegimenContributivo_PAR>(filtro);
    				return ConsultarTipoRegimenContributivo_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de TipoRegimenContributivo_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<TipoRegimenContributivo_PAR> ConsultarTipoRegimenContributivo_PAR(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<TipoRegimenContributivo_PAR, bool>> expresionFiltrado = ObtenerFiltro<TipoRegimenContributivo_PAR>(filtros,where);
    				return ConsultarTipoRegimenContributivo_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de TipoRegimenContributivo_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<TipoRegimenContributivo_PAR> ConsultarTipoRegimenContributivo_PAR(Expression<Func<TipoRegimenContributivo_PAR, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<TipoRegimenContributivo_PAR>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de TipoRegimenContributivo_PAR que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<TipoRegimenContributivo_PAR> ConsultarTipoRegimenContributivo_PAR(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<TipoRegimenContributivo_PAR>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de TipoSegmentoMercado_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<TipoSegmentoMercado_PAR> ConsultarContainsTipoSegmentoMercado_PAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<TipoSegmentoMercado_PAR, bool>> expresionFiltrado = ObtenerExpresionContains<TipoSegmentoMercado_PAR>(filtro);
    			return ConsultarTipoSegmentoMercado_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de TipoSegmentoMercado_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<TipoSegmentoMercado_PAR> ConsultarEqualsTipoSegmentoMercado_PAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<TipoSegmentoMercado_PAR, bool>> expresionFiltrado = ObtenerExpresionEquals<TipoSegmentoMercado_PAR>(filtro);
    				return ConsultarTipoSegmentoMercado_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de TipoSegmentoMercado_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<TipoSegmentoMercado_PAR> ConsultarTipoSegmentoMercado_PAR(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<TipoSegmentoMercado_PAR, bool>> expresionFiltrado = ObtenerFiltro<TipoSegmentoMercado_PAR>(filtros,where);
    				return ConsultarTipoSegmentoMercado_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de TipoSegmentoMercado_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<TipoSegmentoMercado_PAR> ConsultarTipoSegmentoMercado_PAR(Expression<Func<TipoSegmentoMercado_PAR, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<TipoSegmentoMercado_PAR>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de TipoSegmentoMercado_PAR que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<TipoSegmentoMercado_PAR> ConsultarTipoSegmentoMercado_PAR(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<TipoSegmentoMercado_PAR>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de TipoSociedad_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<TipoSociedad_PAR> ConsultarContainsTipoSociedad_PAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<TipoSociedad_PAR, bool>> expresionFiltrado = ObtenerExpresionContains<TipoSociedad_PAR>(filtro);
    			return ConsultarTipoSociedad_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de TipoSociedad_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<TipoSociedad_PAR> ConsultarEqualsTipoSociedad_PAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<TipoSociedad_PAR, bool>> expresionFiltrado = ObtenerExpresionEquals<TipoSociedad_PAR>(filtro);
    				return ConsultarTipoSociedad_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de TipoSociedad_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<TipoSociedad_PAR> ConsultarTipoSociedad_PAR(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<TipoSociedad_PAR, bool>> expresionFiltrado = ObtenerFiltro<TipoSociedad_PAR>(filtros,where);
    				return ConsultarTipoSociedad_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de TipoSociedad_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<TipoSociedad_PAR> ConsultarTipoSociedad_PAR(Expression<Func<TipoSociedad_PAR, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<TipoSociedad_PAR>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de TipoSociedad_PAR que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<TipoSociedad_PAR> ConsultarTipoSociedad_PAR(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<TipoSociedad_PAR>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de Localidades_VPAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<Localidades_VPAR> ConsultarContainsLocalidades_VPAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<Localidades_VPAR, bool>> expresionFiltrado = ObtenerExpresionContains<Localidades_VPAR>(filtro);
    			return ConsultarLocalidades_VPAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de Localidades_VPAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Localidades_VPAR> ConsultarEqualsLocalidades_VPAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<Localidades_VPAR, bool>> expresionFiltrado = ObtenerExpresionEquals<Localidades_VPAR>(filtro);
    				return ConsultarLocalidades_VPAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de Localidades_VPAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Localidades_VPAR> ConsultarLocalidades_VPAR(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<Localidades_VPAR, bool>> expresionFiltrado = ObtenerFiltro<Localidades_VPAR>(filtros,where);
    				return ConsultarLocalidades_VPAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de Localidades_VPAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Localidades_VPAR> ConsultarLocalidades_VPAR(Expression<Func<Localidades_VPAR, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<Localidades_VPAR>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de Localidades_VPAR que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Localidades_VPAR> ConsultarLocalidades_VPAR(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<Localidades_VPAR>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de MotivoEstados_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<MotivoEstados_PAR> ConsultarContainsMotivoEstados_PAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<MotivoEstados_PAR, bool>> expresionFiltrado = ObtenerExpresionContains<MotivoEstados_PAR>(filtro);
    			return ConsultarMotivoEstados_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de MotivoEstados_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<MotivoEstados_PAR> ConsultarEqualsMotivoEstados_PAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<MotivoEstados_PAR, bool>> expresionFiltrado = ObtenerExpresionEquals<MotivoEstados_PAR>(filtro);
    				return ConsultarMotivoEstados_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de MotivoEstados_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<MotivoEstados_PAR> ConsultarMotivoEstados_PAR(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<MotivoEstados_PAR, bool>> expresionFiltrado = ObtenerFiltro<MotivoEstados_PAR>(filtros,where);
    				return ConsultarMotivoEstados_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de MotivoEstados_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<MotivoEstados_PAR> ConsultarMotivoEstados_PAR(Expression<Func<MotivoEstados_PAR, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<MotivoEstados_PAR>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de MotivoEstados_PAR que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<MotivoEstados_PAR> ConsultarMotivoEstados_PAR(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<MotivoEstados_PAR>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de PersonaResponsableLegal_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<PersonaResponsableLegal_PAR> ConsultarContainsPersonaResponsableLegal_PAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<PersonaResponsableLegal_PAR, bool>> expresionFiltrado = ObtenerExpresionContains<PersonaResponsableLegal_PAR>(filtro);
    			return ConsultarPersonaResponsableLegal_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de PersonaResponsableLegal_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<PersonaResponsableLegal_PAR> ConsultarEqualsPersonaResponsableLegal_PAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<PersonaResponsableLegal_PAR, bool>> expresionFiltrado = ObtenerExpresionEquals<PersonaResponsableLegal_PAR>(filtro);
    				return ConsultarPersonaResponsableLegal_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de PersonaResponsableLegal_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<PersonaResponsableLegal_PAR> ConsultarPersonaResponsableLegal_PAR(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<PersonaResponsableLegal_PAR, bool>> expresionFiltrado = ObtenerFiltro<PersonaResponsableLegal_PAR>(filtros,where);
    				return ConsultarPersonaResponsableLegal_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de PersonaResponsableLegal_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<PersonaResponsableLegal_PAR> ConsultarPersonaResponsableLegal_PAR(Expression<Func<PersonaResponsableLegal_PAR, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<PersonaResponsableLegal_PAR>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de PersonaResponsableLegal_PAR que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<PersonaResponsableLegal_PAR> ConsultarPersonaResponsableLegal_PAR(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<PersonaResponsableLegal_PAR>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de PersonaExterna_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<PersonaExterna_PAR> ConsultarContainsPersonaExterna_PAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<PersonaExterna_PAR, bool>> expresionFiltrado = ObtenerExpresionContains<PersonaExterna_PAR>(filtro);
    			return ConsultarPersonaExterna_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de PersonaExterna_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<PersonaExterna_PAR> ConsultarEqualsPersonaExterna_PAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<PersonaExterna_PAR, bool>> expresionFiltrado = ObtenerExpresionEquals<PersonaExterna_PAR>(filtro);
    				return ConsultarPersonaExterna_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de PersonaExterna_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<PersonaExterna_PAR> ConsultarPersonaExterna_PAR(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<PersonaExterna_PAR, bool>> expresionFiltrado = ObtenerFiltro<PersonaExterna_PAR>(filtros,where);
    				return ConsultarPersonaExterna_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de PersonaExterna_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<PersonaExterna_PAR> ConsultarPersonaExterna_PAR(Expression<Func<PersonaExterna_PAR, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<PersonaExterna_PAR>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de PersonaExterna_PAR que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<PersonaExterna_PAR> ConsultarPersonaExterna_PAR(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<PersonaExterna_PAR>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de DocumentosClienteCredito_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<DocumentosClienteCredito_VCLI> ConsultarContainsDocumentosClienteCredito_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<DocumentosClienteCredito_VCLI, bool>> expresionFiltrado = ObtenerExpresionContains<DocumentosClienteCredito_VCLI>(filtro);
    			return ConsultarDocumentosClienteCredito_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de DocumentosClienteCredito_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<DocumentosClienteCredito_VCLI> ConsultarEqualsDocumentosClienteCredito_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<DocumentosClienteCredito_VCLI, bool>> expresionFiltrado = ObtenerExpresionEquals<DocumentosClienteCredito_VCLI>(filtro);
    				return ConsultarDocumentosClienteCredito_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de DocumentosClienteCredito_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<DocumentosClienteCredito_VCLI> ConsultarDocumentosClienteCredito_VCLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<DocumentosClienteCredito_VCLI, bool>> expresionFiltrado = ObtenerFiltro<DocumentosClienteCredito_VCLI>(filtros,where);
    				return ConsultarDocumentosClienteCredito_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de DocumentosClienteCredito_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<DocumentosClienteCredito_VCLI> ConsultarDocumentosClienteCredito_VCLI(Expression<Func<DocumentosClienteCredito_VCLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<DocumentosClienteCredito_VCLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de DocumentosClienteCredito_VCLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<DocumentosClienteCredito_VCLI> ConsultarDocumentosClienteCredito_VCLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<DocumentosClienteCredito_VCLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de AcumuladoGirosCliente_GIR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<AcumuladoGirosCliente_GIR> ConsultarContainsAcumuladoGirosCliente_GIR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<AcumuladoGirosCliente_GIR, bool>> expresionFiltrado = ObtenerExpresionContains<AcumuladoGirosCliente_GIR>(filtro);
    			return ConsultarAcumuladoGirosCliente_GIR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de AcumuladoGirosCliente_GIR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<AcumuladoGirosCliente_GIR> ConsultarEqualsAcumuladoGirosCliente_GIR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<AcumuladoGirosCliente_GIR, bool>> expresionFiltrado = ObtenerExpresionEquals<AcumuladoGirosCliente_GIR>(filtro);
    				return ConsultarAcumuladoGirosCliente_GIR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de AcumuladoGirosCliente_GIR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<AcumuladoGirosCliente_GIR> ConsultarAcumuladoGirosCliente_GIR(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<AcumuladoGirosCliente_GIR, bool>> expresionFiltrado = ObtenerFiltro<AcumuladoGirosCliente_GIR>(filtros,where);
    				return ConsultarAcumuladoGirosCliente_GIR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de AcumuladoGirosCliente_GIR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<AcumuladoGirosCliente_GIR> ConsultarAcumuladoGirosCliente_GIR(Expression<Func<AcumuladoGirosCliente_GIR, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<AcumuladoGirosCliente_GIR>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de AcumuladoGirosCliente_GIR que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<AcumuladoGirosCliente_GIR> ConsultarAcumuladoGirosCliente_GIR(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<AcumuladoGirosCliente_GIR>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de Ocupacion_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<Ocupacion_PAR> ConsultarContainsOcupacion_PAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<Ocupacion_PAR, bool>> expresionFiltrado = ObtenerExpresionContains<Ocupacion_PAR>(filtro);
    			return ConsultarOcupacion_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de Ocupacion_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Ocupacion_PAR> ConsultarEqualsOcupacion_PAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<Ocupacion_PAR, bool>> expresionFiltrado = ObtenerExpresionEquals<Ocupacion_PAR>(filtro);
    				return ConsultarOcupacion_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de Ocupacion_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Ocupacion_PAR> ConsultarOcupacion_PAR(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<Ocupacion_PAR, bool>> expresionFiltrado = ObtenerFiltro<Ocupacion_PAR>(filtros,where);
    				return ConsultarOcupacion_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de Ocupacion_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Ocupacion_PAR> ConsultarOcupacion_PAR(Expression<Func<Ocupacion_PAR, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<Ocupacion_PAR>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de Ocupacion_PAR que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Ocupacion_PAR> ConsultarOcupacion_PAR(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<Ocupacion_PAR>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de DocumentosClienteCredito_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<DocumentosClienteCredito_CLI> ConsultarContainsDocumentosClienteCredito_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<DocumentosClienteCredito_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<DocumentosClienteCredito_CLI>(filtro);
    			return ConsultarDocumentosClienteCredito_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de DocumentosClienteCredito_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<DocumentosClienteCredito_CLI> ConsultarEqualsDocumentosClienteCredito_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<DocumentosClienteCredito_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<DocumentosClienteCredito_CLI>(filtro);
    				return ConsultarDocumentosClienteCredito_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de DocumentosClienteCredito_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<DocumentosClienteCredito_CLI> ConsultarDocumentosClienteCredito_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<DocumentosClienteCredito_CLI, bool>> expresionFiltrado = ObtenerFiltro<DocumentosClienteCredito_CLI>(filtros,where);
    				return ConsultarDocumentosClienteCredito_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de DocumentosClienteCredito_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<DocumentosClienteCredito_CLI> ConsultarDocumentosClienteCredito_CLI(Expression<Func<DocumentosClienteCredito_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<DocumentosClienteCredito_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de DocumentosClienteCredito_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<DocumentosClienteCredito_CLI> ConsultarDocumentosClienteCredito_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<DocumentosClienteCredito_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de DestinatarioFrecuente_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<DestinatarioFrecuente_CLI> ConsultarContainsDestinatarioFrecuente_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<DestinatarioFrecuente_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<DestinatarioFrecuente_CLI>(filtro);
    			return ConsultarDestinatarioFrecuente_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de DestinatarioFrecuente_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<DestinatarioFrecuente_CLI> ConsultarEqualsDestinatarioFrecuente_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<DestinatarioFrecuente_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<DestinatarioFrecuente_CLI>(filtro);
    				return ConsultarDestinatarioFrecuente_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de DestinatarioFrecuente_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<DestinatarioFrecuente_CLI> ConsultarDestinatarioFrecuente_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<DestinatarioFrecuente_CLI, bool>> expresionFiltrado = ObtenerFiltro<DestinatarioFrecuente_CLI>(filtros,where);
    				return ConsultarDestinatarioFrecuente_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de DestinatarioFrecuente_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<DestinatarioFrecuente_CLI> ConsultarDestinatarioFrecuente_CLI(Expression<Func<DestinatarioFrecuente_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<DestinatarioFrecuente_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de DestinatarioFrecuente_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<DestinatarioFrecuente_CLI> ConsultarDestinatarioFrecuente_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<DestinatarioFrecuente_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de PersonalContrato_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<PersonalContrato_CLI> ConsultarContainsPersonalContrato_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<PersonalContrato_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<PersonalContrato_CLI>(filtro);
    			return ConsultarPersonalContrato_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de PersonalContrato_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<PersonalContrato_CLI> ConsultarEqualsPersonalContrato_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<PersonalContrato_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<PersonalContrato_CLI>(filtro);
    				return ConsultarPersonalContrato_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de PersonalContrato_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<PersonalContrato_CLI> ConsultarPersonalContrato_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<PersonalContrato_CLI, bool>> expresionFiltrado = ObtenerFiltro<PersonalContrato_CLI>(filtros,where);
    				return ConsultarPersonalContrato_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de PersonalContrato_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<PersonalContrato_CLI> ConsultarPersonalContrato_CLI(Expression<Func<PersonalContrato_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<PersonalContrato_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de PersonalContrato_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<PersonalContrato_CLI> ConsultarPersonalContrato_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<PersonalContrato_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de Cargo_SEG que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<Cargo_SEG> ConsultarContainsCargo_SEG(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<Cargo_SEG, bool>> expresionFiltrado = ObtenerExpresionContains<Cargo_SEG>(filtro);
    			return ConsultarCargo_SEG(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de Cargo_SEG que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Cargo_SEG> ConsultarEqualsCargo_SEG(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<Cargo_SEG, bool>> expresionFiltrado = ObtenerExpresionEquals<Cargo_SEG>(filtro);
    				return ConsultarCargo_SEG(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de Cargo_SEG que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Cargo_SEG> ConsultarCargo_SEG(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<Cargo_SEG, bool>> expresionFiltrado = ObtenerFiltro<Cargo_SEG>(filtros,where);
    				return ConsultarCargo_SEG(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de Cargo_SEG que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Cargo_SEG> ConsultarCargo_SEG(Expression<Func<Cargo_SEG, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<Cargo_SEG>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de Cargo_SEG que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Cargo_SEG> ConsultarCargo_SEG(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<Cargo_SEG>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de PersonaInternaCargoCenSer_VPAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<PersonaInternaCargoCenSer_VPAR> ConsultarContainsPersonaInternaCargoCenSer_VPAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<PersonaInternaCargoCenSer_VPAR, bool>> expresionFiltrado = ObtenerExpresionContains<PersonaInternaCargoCenSer_VPAR>(filtro);
    			return ConsultarPersonaInternaCargoCenSer_VPAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de PersonaInternaCargoCenSer_VPAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<PersonaInternaCargoCenSer_VPAR> ConsultarEqualsPersonaInternaCargoCenSer_VPAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<PersonaInternaCargoCenSer_VPAR, bool>> expresionFiltrado = ObtenerExpresionEquals<PersonaInternaCargoCenSer_VPAR>(filtro);
    				return ConsultarPersonaInternaCargoCenSer_VPAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de PersonaInternaCargoCenSer_VPAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<PersonaInternaCargoCenSer_VPAR> ConsultarPersonaInternaCargoCenSer_VPAR(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<PersonaInternaCargoCenSer_VPAR, bool>> expresionFiltrado = ObtenerFiltro<PersonaInternaCargoCenSer_VPAR>(filtros,where);
    				return ConsultarPersonaInternaCargoCenSer_VPAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de PersonaInternaCargoCenSer_VPAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<PersonaInternaCargoCenSer_VPAR> ConsultarPersonaInternaCargoCenSer_VPAR(Expression<Func<PersonaInternaCargoCenSer_VPAR, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<PersonaInternaCargoCenSer_VPAR>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de PersonaInternaCargoCenSer_VPAR que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<PersonaInternaCargoCenSer_VPAR> ConsultarPersonaInternaCargoCenSer_VPAR(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<PersonaInternaCargoCenSer_VPAR>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de ModalidadOtroSi_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<ModalidadOtroSi_CLI> ConsultarContainsModalidadOtroSi_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<ModalidadOtroSi_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<ModalidadOtroSi_CLI>(filtro);
    			return ConsultarModalidadOtroSi_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ModalidadOtroSi_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ModalidadOtroSi_CLI> ConsultarEqualsModalidadOtroSi_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ModalidadOtroSi_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<ModalidadOtroSi_CLI>(filtro);
    				return ConsultarModalidadOtroSi_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ModalidadOtroSi_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ModalidadOtroSi_CLI> ConsultarModalidadOtroSi_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ModalidadOtroSi_CLI, bool>> expresionFiltrado = ObtenerFiltro<ModalidadOtroSi_CLI>(filtros,where);
    				return ConsultarModalidadOtroSi_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ModalidadOtroSi_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ModalidadOtroSi_CLI> ConsultarModalidadOtroSi_CLI(Expression<Func<ModalidadOtroSi_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ModalidadOtroSi_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de ModalidadOtroSi_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ModalidadOtroSi_CLI> ConsultarModalidadOtroSi_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ModalidadOtroSi_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de ClienteContadoHist_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<ClienteContadoHist_CLI> ConsultarContainsClienteContadoHist_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<ClienteContadoHist_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<ClienteContadoHist_CLI>(filtro);
    			return ConsultarClienteContadoHist_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ClienteContadoHist_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClienteContadoHist_CLI> ConsultarEqualsClienteContadoHist_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ClienteContadoHist_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<ClienteContadoHist_CLI>(filtro);
    				return ConsultarClienteContadoHist_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ClienteContadoHist_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClienteContadoHist_CLI> ConsultarClienteContadoHist_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ClienteContadoHist_CLI, bool>> expresionFiltrado = ObtenerFiltro<ClienteContadoHist_CLI>(filtros,where);
    				return ConsultarClienteContadoHist_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ClienteContadoHist_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClienteContadoHist_CLI> ConsultarClienteContadoHist_CLI(Expression<Func<ClienteContadoHist_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ClienteContadoHist_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de ClienteContadoHist_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClienteContadoHist_CLI> ConsultarClienteContadoHist_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ClienteContadoHist_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de DocumentosContrato_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<DocumentosContrato_CLI> ConsultarContainsDocumentosContrato_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<DocumentosContrato_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<DocumentosContrato_CLI>(filtro);
    			return ConsultarDocumentosContrato_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de DocumentosContrato_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<DocumentosContrato_CLI> ConsultarEqualsDocumentosContrato_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<DocumentosContrato_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<DocumentosContrato_CLI>(filtro);
    				return ConsultarDocumentosContrato_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de DocumentosContrato_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<DocumentosContrato_CLI> ConsultarDocumentosContrato_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<DocumentosContrato_CLI, bool>> expresionFiltrado = ObtenerFiltro<DocumentosContrato_CLI>(filtros,where);
    				return ConsultarDocumentosContrato_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de DocumentosContrato_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<DocumentosContrato_CLI> ConsultarDocumentosContrato_CLI(Expression<Func<DocumentosContrato_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<DocumentosContrato_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de DocumentosContrato_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<DocumentosContrato_CLI> ConsultarDocumentosContrato_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<DocumentosContrato_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de HorarioRecogidaSucursalContrato_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<HorarioRecogidaSucursalContrato_CLI> ConsultarContainsHorarioRecogidaSucursalContrato_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<HorarioRecogidaSucursalContrato_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<HorarioRecogidaSucursalContrato_CLI>(filtro);
    			return ConsultarHorarioRecogidaSucursalContrato_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de HorarioRecogidaSucursalContrato_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<HorarioRecogidaSucursalContrato_CLI> ConsultarEqualsHorarioRecogidaSucursalContrato_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<HorarioRecogidaSucursalContrato_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<HorarioRecogidaSucursalContrato_CLI>(filtro);
    				return ConsultarHorarioRecogidaSucursalContrato_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de HorarioRecogidaSucursalContrato_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<HorarioRecogidaSucursalContrato_CLI> ConsultarHorarioRecogidaSucursalContrato_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<HorarioRecogidaSucursalContrato_CLI, bool>> expresionFiltrado = ObtenerFiltro<HorarioRecogidaSucursalContrato_CLI>(filtros,where);
    				return ConsultarHorarioRecogidaSucursalContrato_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de HorarioRecogidaSucursalContrato_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<HorarioRecogidaSucursalContrato_CLI> ConsultarHorarioRecogidaSucursalContrato_CLI(Expression<Func<HorarioRecogidaSucursalContrato_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<HorarioRecogidaSucursalContrato_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de HorarioRecogidaSucursalContrato_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<HorarioRecogidaSucursalContrato_CLI> ConsultarHorarioRecogidaSucursalContrato_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<HorarioRecogidaSucursalContrato_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de SucursalesContratoHist_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<SucursalesContratoHist_CLI> ConsultarContainsSucursalesContratoHist_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<SucursalesContratoHist_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<SucursalesContratoHist_CLI>(filtro);
    			return ConsultarSucursalesContratoHist_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de SucursalesContratoHist_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SucursalesContratoHist_CLI> ConsultarEqualsSucursalesContratoHist_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<SucursalesContratoHist_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<SucursalesContratoHist_CLI>(filtro);
    				return ConsultarSucursalesContratoHist_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de SucursalesContratoHist_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SucursalesContratoHist_CLI> ConsultarSucursalesContratoHist_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<SucursalesContratoHist_CLI, bool>> expresionFiltrado = ObtenerFiltro<SucursalesContratoHist_CLI>(filtros,where);
    				return ConsultarSucursalesContratoHist_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de SucursalesContratoHist_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SucursalesContratoHist_CLI> ConsultarSucursalesContratoHist_CLI(Expression<Func<SucursalesContratoHist_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<SucursalesContratoHist_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de SucursalesContratoHist_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SucursalesContratoHist_CLI> ConsultarSucursalesContratoHist_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<SucursalesContratoHist_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de Zona_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<Zona_PAR> ConsultarContainsZona_PAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<Zona_PAR, bool>> expresionFiltrado = ObtenerExpresionContains<Zona_PAR>(filtro);
    			return ConsultarZona_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de Zona_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Zona_PAR> ConsultarEqualsZona_PAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<Zona_PAR, bool>> expresionFiltrado = ObtenerExpresionEquals<Zona_PAR>(filtro);
    				return ConsultarZona_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de Zona_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Zona_PAR> ConsultarZona_PAR(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<Zona_PAR, bool>> expresionFiltrado = ObtenerFiltro<Zona_PAR>(filtros,where);
    				return ConsultarZona_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de Zona_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Zona_PAR> ConsultarZona_PAR(Expression<Func<Zona_PAR, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<Zona_PAR>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de Zona_PAR que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Zona_PAR> ConsultarZona_PAR(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<Zona_PAR>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de ZonaDeLocalidad_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<ZonaDeLocalidad_PAR> ConsultarContainsZonaDeLocalidad_PAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<ZonaDeLocalidad_PAR, bool>> expresionFiltrado = ObtenerExpresionContains<ZonaDeLocalidad_PAR>(filtro);
    			return ConsultarZonaDeLocalidad_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ZonaDeLocalidad_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ZonaDeLocalidad_PAR> ConsultarEqualsZonaDeLocalidad_PAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ZonaDeLocalidad_PAR, bool>> expresionFiltrado = ObtenerExpresionEquals<ZonaDeLocalidad_PAR>(filtro);
    				return ConsultarZonaDeLocalidad_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ZonaDeLocalidad_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ZonaDeLocalidad_PAR> ConsultarZonaDeLocalidad_PAR(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ZonaDeLocalidad_PAR, bool>> expresionFiltrado = ObtenerFiltro<ZonaDeLocalidad_PAR>(filtros,where);
    				return ConsultarZonaDeLocalidad_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ZonaDeLocalidad_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ZonaDeLocalidad_PAR> ConsultarZonaDeLocalidad_PAR(Expression<Func<ZonaDeLocalidad_PAR, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ZonaDeLocalidad_PAR>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de ZonaDeLocalidad_PAR que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ZonaDeLocalidad_PAR> ConsultarZonaDeLocalidad_PAR(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ZonaDeLocalidad_PAR>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de TipoNotacionFacturacion_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<TipoNotacionFacturacion_CLI> ConsultarContainsTipoNotacionFacturacion_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<TipoNotacionFacturacion_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<TipoNotacionFacturacion_CLI>(filtro);
    			return ConsultarTipoNotacionFacturacion_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de TipoNotacionFacturacion_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<TipoNotacionFacturacion_CLI> ConsultarEqualsTipoNotacionFacturacion_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<TipoNotacionFacturacion_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<TipoNotacionFacturacion_CLI>(filtro);
    				return ConsultarTipoNotacionFacturacion_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de TipoNotacionFacturacion_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<TipoNotacionFacturacion_CLI> ConsultarTipoNotacionFacturacion_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<TipoNotacionFacturacion_CLI, bool>> expresionFiltrado = ObtenerFiltro<TipoNotacionFacturacion_CLI>(filtros,where);
    				return ConsultarTipoNotacionFacturacion_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de TipoNotacionFacturacion_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<TipoNotacionFacturacion_CLI> ConsultarTipoNotacionFacturacion_CLI(Expression<Func<TipoNotacionFacturacion_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<TipoNotacionFacturacion_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de TipoNotacionFacturacion_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<TipoNotacionFacturacion_CLI> ConsultarTipoNotacionFacturacion_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<TipoNotacionFacturacion_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de NotacionesFacturacion_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<NotacionesFacturacion_CLI> ConsultarContainsNotacionesFacturacion_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<NotacionesFacturacion_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<NotacionesFacturacion_CLI>(filtro);
    			return ConsultarNotacionesFacturacion_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de NotacionesFacturacion_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<NotacionesFacturacion_CLI> ConsultarEqualsNotacionesFacturacion_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<NotacionesFacturacion_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<NotacionesFacturacion_CLI>(filtro);
    				return ConsultarNotacionesFacturacion_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de NotacionesFacturacion_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<NotacionesFacturacion_CLI> ConsultarNotacionesFacturacion_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<NotacionesFacturacion_CLI, bool>> expresionFiltrado = ObtenerFiltro<NotacionesFacturacion_CLI>(filtros,where);
    				return ConsultarNotacionesFacturacion_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de NotacionesFacturacion_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<NotacionesFacturacion_CLI> ConsultarNotacionesFacturacion_CLI(Expression<Func<NotacionesFacturacion_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<NotacionesFacturacion_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de NotacionesFacturacion_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<NotacionesFacturacion_CLI> ConsultarNotacionesFacturacion_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<NotacionesFacturacion_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de AgrupacionFactura_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<AgrupacionFactura_CLI> ConsultarContainsAgrupacionFactura_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<AgrupacionFactura_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<AgrupacionFactura_CLI>(filtro);
    			return ConsultarAgrupacionFactura_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de AgrupacionFactura_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<AgrupacionFactura_CLI> ConsultarEqualsAgrupacionFactura_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<AgrupacionFactura_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<AgrupacionFactura_CLI>(filtro);
    				return ConsultarAgrupacionFactura_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de AgrupacionFactura_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<AgrupacionFactura_CLI> ConsultarAgrupacionFactura_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<AgrupacionFactura_CLI, bool>> expresionFiltrado = ObtenerFiltro<AgrupacionFactura_CLI>(filtros,where);
    				return ConsultarAgrupacionFactura_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de AgrupacionFactura_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<AgrupacionFactura_CLI> ConsultarAgrupacionFactura_CLI(Expression<Func<AgrupacionFactura_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<AgrupacionFactura_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de AgrupacionFactura_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<AgrupacionFactura_CLI> ConsultarAgrupacionFactura_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<AgrupacionFactura_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de Servicio_TAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<Servicio_TAR> ConsultarContainsServicio_TAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<Servicio_TAR, bool>> expresionFiltrado = ObtenerExpresionContains<Servicio_TAR>(filtro);
    			return ConsultarServicio_TAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de Servicio_TAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Servicio_TAR> ConsultarEqualsServicio_TAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<Servicio_TAR, bool>> expresionFiltrado = ObtenerExpresionEquals<Servicio_TAR>(filtro);
    				return ConsultarServicio_TAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de Servicio_TAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Servicio_TAR> ConsultarServicio_TAR(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<Servicio_TAR, bool>> expresionFiltrado = ObtenerFiltro<Servicio_TAR>(filtros,where);
    				return ConsultarServicio_TAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de Servicio_TAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Servicio_TAR> ConsultarServicio_TAR(Expression<Func<Servicio_TAR, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<Servicio_TAR>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de Servicio_TAR que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Servicio_TAR> ConsultarServicio_TAR(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<Servicio_TAR>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de AgrupacionFacturaSvcSucur_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<AgrupacionFacturaSvcSucur_VCLI> ConsultarContainsAgrupacionFacturaSvcSucur_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<AgrupacionFacturaSvcSucur_VCLI, bool>> expresionFiltrado = ObtenerExpresionContains<AgrupacionFacturaSvcSucur_VCLI>(filtro);
    			return ConsultarAgrupacionFacturaSvcSucur_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de AgrupacionFacturaSvcSucur_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<AgrupacionFacturaSvcSucur_VCLI> ConsultarEqualsAgrupacionFacturaSvcSucur_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<AgrupacionFacturaSvcSucur_VCLI, bool>> expresionFiltrado = ObtenerExpresionEquals<AgrupacionFacturaSvcSucur_VCLI>(filtro);
    				return ConsultarAgrupacionFacturaSvcSucur_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de AgrupacionFacturaSvcSucur_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<AgrupacionFacturaSvcSucur_VCLI> ConsultarAgrupacionFacturaSvcSucur_VCLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<AgrupacionFacturaSvcSucur_VCLI, bool>> expresionFiltrado = ObtenerFiltro<AgrupacionFacturaSvcSucur_VCLI>(filtros,where);
    				return ConsultarAgrupacionFacturaSvcSucur_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de AgrupacionFacturaSvcSucur_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<AgrupacionFacturaSvcSucur_VCLI> ConsultarAgrupacionFacturaSvcSucur_VCLI(Expression<Func<AgrupacionFacturaSvcSucur_VCLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<AgrupacionFacturaSvcSucur_VCLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de AgrupacionFacturaSvcSucur_VCLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<AgrupacionFacturaSvcSucur_VCLI> ConsultarAgrupacionFacturaSvcSucur_VCLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<AgrupacionFacturaSvcSucur_VCLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de VersionServicios_VVER que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<VersionServicios_VVER> ConsultarContainsVersionServicios_VVER(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<VersionServicios_VVER, bool>> expresionFiltrado = ObtenerExpresionContains<VersionServicios_VVER>(filtro);
    			return ConsultarVersionServicios_VVER(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de VersionServicios_VVER que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<VersionServicios_VVER> ConsultarEqualsVersionServicios_VVER(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<VersionServicios_VVER, bool>> expresionFiltrado = ObtenerExpresionEquals<VersionServicios_VVER>(filtro);
    				return ConsultarVersionServicios_VVER(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de VersionServicios_VVER que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<VersionServicios_VVER> ConsultarVersionServicios_VVER(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<VersionServicios_VVER, bool>> expresionFiltrado = ObtenerFiltro<VersionServicios_VVER>(filtros,where);
    				return ConsultarVersionServicios_VVER(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de VersionServicios_VVER que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<VersionServicios_VVER> ConsultarVersionServicios_VVER(Expression<Func<VersionServicios_VVER, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<VersionServicios_VVER>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de VersionServicios_VVER que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<VersionServicios_VVER> ConsultarVersionServicios_VVER(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<VersionServicios_VVER>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de AcumuladosContrato_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<AcumuladosContrato_CLI> ConsultarContainsAcumuladosContrato_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<AcumuladosContrato_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<AcumuladosContrato_CLI>(filtro);
    			return ConsultarAcumuladosContrato_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de AcumuladosContrato_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<AcumuladosContrato_CLI> ConsultarEqualsAcumuladosContrato_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<AcumuladosContrato_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<AcumuladosContrato_CLI>(filtro);
    				return ConsultarAcumuladosContrato_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de AcumuladosContrato_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<AcumuladosContrato_CLI> ConsultarAcumuladosContrato_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<AcumuladosContrato_CLI, bool>> expresionFiltrado = ObtenerFiltro<AcumuladosContrato_CLI>(filtros,where);
    				return ConsultarAcumuladosContrato_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de AcumuladosContrato_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<AcumuladosContrato_CLI> ConsultarAcumuladosContrato_CLI(Expression<Func<AcumuladosContrato_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<AcumuladosContrato_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de AcumuladosContrato_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<AcumuladosContrato_CLI> ConsultarAcumuladosContrato_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<AcumuladosContrato_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de CliContadoArchivoCedulas_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<CliContadoArchivoCedulas_VCLI> ConsultarContainsCliContadoArchivoCedulas_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<CliContadoArchivoCedulas_VCLI, bool>> expresionFiltrado = ObtenerExpresionContains<CliContadoArchivoCedulas_VCLI>(filtro);
    			return ConsultarCliContadoArchivoCedulas_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de CliContadoArchivoCedulas_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<CliContadoArchivoCedulas_VCLI> ConsultarEqualsCliContadoArchivoCedulas_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<CliContadoArchivoCedulas_VCLI, bool>> expresionFiltrado = ObtenerExpresionEquals<CliContadoArchivoCedulas_VCLI>(filtro);
    				return ConsultarCliContadoArchivoCedulas_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de CliContadoArchivoCedulas_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<CliContadoArchivoCedulas_VCLI> ConsultarCliContadoArchivoCedulas_VCLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<CliContadoArchivoCedulas_VCLI, bool>> expresionFiltrado = ObtenerFiltro<CliContadoArchivoCedulas_VCLI>(filtros,where);
    				return ConsultarCliContadoArchivoCedulas_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de CliContadoArchivoCedulas_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<CliContadoArchivoCedulas_VCLI> ConsultarCliContadoArchivoCedulas_VCLI(Expression<Func<CliContadoArchivoCedulas_VCLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<CliContadoArchivoCedulas_VCLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de CliContadoArchivoCedulas_VCLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<CliContadoArchivoCedulas_VCLI> ConsultarCliContadoArchivoCedulas_VCLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<CliContadoArchivoCedulas_VCLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de ListaPrecios_TAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<ListaPrecios_TAR> ConsultarContainsListaPrecios_TAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<ListaPrecios_TAR, bool>> expresionFiltrado = ObtenerExpresionContains<ListaPrecios_TAR>(filtro);
    			return ConsultarListaPrecios_TAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ListaPrecios_TAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ListaPrecios_TAR> ConsultarEqualsListaPrecios_TAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ListaPrecios_TAR, bool>> expresionFiltrado = ObtenerExpresionEquals<ListaPrecios_TAR>(filtro);
    				return ConsultarListaPrecios_TAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ListaPrecios_TAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ListaPrecios_TAR> ConsultarListaPrecios_TAR(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ListaPrecios_TAR, bool>> expresionFiltrado = ObtenerFiltro<ListaPrecios_TAR>(filtros,where);
    				return ConsultarListaPrecios_TAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ListaPrecios_TAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ListaPrecios_TAR> ConsultarListaPrecios_TAR(Expression<Func<ListaPrecios_TAR, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ListaPrecios_TAR>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de ListaPrecios_TAR que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ListaPrecios_TAR> ConsultarListaPrecios_TAR(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ListaPrecios_TAR>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de SucursalServicios_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<SucursalServicios_VCLI> ConsultarContainsSucursalServicios_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<SucursalServicios_VCLI, bool>> expresionFiltrado = ObtenerExpresionContains<SucursalServicios_VCLI>(filtro);
    			return ConsultarSucursalServicios_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de SucursalServicios_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SucursalServicios_VCLI> ConsultarEqualsSucursalServicios_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<SucursalServicios_VCLI, bool>> expresionFiltrado = ObtenerExpresionEquals<SucursalServicios_VCLI>(filtro);
    				return ConsultarSucursalServicios_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de SucursalServicios_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SucursalServicios_VCLI> ConsultarSucursalServicios_VCLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<SucursalServicios_VCLI, bool>> expresionFiltrado = ObtenerFiltro<SucursalServicios_VCLI>(filtros,where);
    				return ConsultarSucursalServicios_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de SucursalServicios_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SucursalServicios_VCLI> ConsultarSucursalServicios_VCLI(Expression<Func<SucursalServicios_VCLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<SucursalServicios_VCLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de SucursalServicios_VCLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SucursalServicios_VCLI> ConsultarSucursalServicios_VCLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<SucursalServicios_VCLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de SucursalesAgencia_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<SucursalesAgencia_VCLI> ConsultarContainsSucursalesAgencia_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<SucursalesAgencia_VCLI, bool>> expresionFiltrado = ObtenerExpresionContains<SucursalesAgencia_VCLI>(filtro);
    			return ConsultarSucursalesAgencia_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de SucursalesAgencia_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SucursalesAgencia_VCLI> ConsultarEqualsSucursalesAgencia_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<SucursalesAgencia_VCLI, bool>> expresionFiltrado = ObtenerExpresionEquals<SucursalesAgencia_VCLI>(filtro);
    				return ConsultarSucursalesAgencia_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de SucursalesAgencia_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SucursalesAgencia_VCLI> ConsultarSucursalesAgencia_VCLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<SucursalesAgencia_VCLI, bool>> expresionFiltrado = ObtenerFiltro<SucursalesAgencia_VCLI>(filtros,where);
    				return ConsultarSucursalesAgencia_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de SucursalesAgencia_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SucursalesAgencia_VCLI> ConsultarSucursalesAgencia_VCLI(Expression<Func<SucursalesAgencia_VCLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<SucursalesAgencia_VCLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de SucursalesAgencia_VCLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SucursalesAgencia_VCLI> ConsultarSucursalesAgencia_VCLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<SucursalesAgencia_VCLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de SucursalesClienteRacol_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<SucursalesClienteRacol_VCLI> ConsultarContainsSucursalesClienteRacol_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<SucursalesClienteRacol_VCLI, bool>> expresionFiltrado = ObtenerExpresionContains<SucursalesClienteRacol_VCLI>(filtro);
    			return ConsultarSucursalesClienteRacol_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de SucursalesClienteRacol_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SucursalesClienteRacol_VCLI> ConsultarEqualsSucursalesClienteRacol_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<SucursalesClienteRacol_VCLI, bool>> expresionFiltrado = ObtenerExpresionEquals<SucursalesClienteRacol_VCLI>(filtro);
    				return ConsultarSucursalesClienteRacol_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de SucursalesClienteRacol_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SucursalesClienteRacol_VCLI> ConsultarSucursalesClienteRacol_VCLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<SucursalesClienteRacol_VCLI, bool>> expresionFiltrado = ObtenerFiltro<SucursalesClienteRacol_VCLI>(filtros,where);
    				return ConsultarSucursalesClienteRacol_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de SucursalesClienteRacol_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SucursalesClienteRacol_VCLI> ConsultarSucursalesClienteRacol_VCLI(Expression<Func<SucursalesClienteRacol_VCLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<SucursalesClienteRacol_VCLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de SucursalesClienteRacol_VCLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SucursalesClienteRacol_VCLI> ConsultarSucursalesClienteRacol_VCLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<SucursalesClienteRacol_VCLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de EstadosCliente_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<EstadosCliente_CLI> ConsultarContainsEstadosCliente_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<EstadosCliente_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<EstadosCliente_CLI>(filtro);
    			return ConsultarEstadosCliente_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de EstadosCliente_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<EstadosCliente_CLI> ConsultarEqualsEstadosCliente_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<EstadosCliente_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<EstadosCliente_CLI>(filtro);
    				return ConsultarEstadosCliente_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de EstadosCliente_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<EstadosCliente_CLI> ConsultarEstadosCliente_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<EstadosCliente_CLI, bool>> expresionFiltrado = ObtenerFiltro<EstadosCliente_CLI>(filtros,where);
    				return ConsultarEstadosCliente_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de EstadosCliente_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<EstadosCliente_CLI> ConsultarEstadosCliente_CLI(Expression<Func<EstadosCliente_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<EstadosCliente_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de EstadosCliente_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<EstadosCliente_CLI> ConsultarEstadosCliente_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<EstadosCliente_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de ClienteCreditoHist_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<ClienteCreditoHist_CLI> ConsultarContainsClienteCreditoHist_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<ClienteCreditoHist_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<ClienteCreditoHist_CLI>(filtro);
    			return ConsultarClienteCreditoHist_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ClienteCreditoHist_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClienteCreditoHist_CLI> ConsultarEqualsClienteCreditoHist_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ClienteCreditoHist_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<ClienteCreditoHist_CLI>(filtro);
    				return ConsultarClienteCreditoHist_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ClienteCreditoHist_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClienteCreditoHist_CLI> ConsultarClienteCreditoHist_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ClienteCreditoHist_CLI, bool>> expresionFiltrado = ObtenerFiltro<ClienteCreditoHist_CLI>(filtros,where);
    				return ConsultarClienteCreditoHist_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ClienteCreditoHist_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClienteCreditoHist_CLI> ConsultarClienteCreditoHist_CLI(Expression<Func<ClienteCreditoHist_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ClienteCreditoHist_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de ClienteCreditoHist_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClienteCreditoHist_CLI> ConsultarClienteCreditoHist_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ClienteCreditoHist_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de SucursalHist_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<SucursalHist_CLI> ConsultarContainsSucursalHist_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<SucursalHist_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<SucursalHist_CLI>(filtro);
    			return ConsultarSucursalHist_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de SucursalHist_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SucursalHist_CLI> ConsultarEqualsSucursalHist_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<SucursalHist_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<SucursalHist_CLI>(filtro);
    				return ConsultarSucursalHist_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de SucursalHist_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SucursalHist_CLI> ConsultarSucursalHist_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<SucursalHist_CLI, bool>> expresionFiltrado = ObtenerFiltro<SucursalHist_CLI>(filtros,where);
    				return ConsultarSucursalHist_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de SucursalHist_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SucursalHist_CLI> ConsultarSucursalHist_CLI(Expression<Func<SucursalHist_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<SucursalHist_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de SucursalHist_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SucursalHist_CLI> ConsultarSucursalHist_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<SucursalHist_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de ContactosContrato_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<ContactosContrato_CLI> ConsultarContainsContactosContrato_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<ContactosContrato_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<ContactosContrato_CLI>(filtro);
    			return ConsultarContactosContrato_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ContactosContrato_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ContactosContrato_CLI> ConsultarEqualsContactosContrato_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ContactosContrato_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<ContactosContrato_CLI>(filtro);
    				return ConsultarContactosContrato_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ContactosContrato_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ContactosContrato_CLI> ConsultarContactosContrato_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ContactosContrato_CLI, bool>> expresionFiltrado = ObtenerFiltro<ContactosContrato_CLI>(filtros,where);
    				return ConsultarContactosContrato_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ContactosContrato_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ContactosContrato_CLI> ConsultarContactosContrato_CLI(Expression<Func<ContactosContrato_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ContactosContrato_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de ContactosContrato_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ContactosContrato_CLI> ConsultarContactosContrato_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ContactosContrato_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de DeduccionesFacturacion_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<DeduccionesFacturacion_CLI> ConsultarContainsDeduccionesFacturacion_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<DeduccionesFacturacion_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<DeduccionesFacturacion_CLI>(filtro);
    			return ConsultarDeduccionesFacturacion_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de DeduccionesFacturacion_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<DeduccionesFacturacion_CLI> ConsultarEqualsDeduccionesFacturacion_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<DeduccionesFacturacion_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<DeduccionesFacturacion_CLI>(filtro);
    				return ConsultarDeduccionesFacturacion_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de DeduccionesFacturacion_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<DeduccionesFacturacion_CLI> ConsultarDeduccionesFacturacion_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<DeduccionesFacturacion_CLI, bool>> expresionFiltrado = ObtenerFiltro<DeduccionesFacturacion_CLI>(filtros,where);
    				return ConsultarDeduccionesFacturacion_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de DeduccionesFacturacion_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<DeduccionesFacturacion_CLI> ConsultarDeduccionesFacturacion_CLI(Expression<Func<DeduccionesFacturacion_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<DeduccionesFacturacion_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de DeduccionesFacturacion_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<DeduccionesFacturacion_CLI> ConsultarDeduccionesFacturacion_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<DeduccionesFacturacion_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de DescuentosFactura_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<DescuentosFactura_CLI> ConsultarContainsDescuentosFactura_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<DescuentosFactura_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<DescuentosFactura_CLI>(filtro);
    			return ConsultarDescuentosFactura_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de DescuentosFactura_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<DescuentosFactura_CLI> ConsultarEqualsDescuentosFactura_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<DescuentosFactura_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<DescuentosFactura_CLI>(filtro);
    				return ConsultarDescuentosFactura_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de DescuentosFactura_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<DescuentosFactura_CLI> ConsultarDescuentosFactura_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<DescuentosFactura_CLI, bool>> expresionFiltrado = ObtenerFiltro<DescuentosFactura_CLI>(filtros,where);
    				return ConsultarDescuentosFactura_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de DescuentosFactura_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<DescuentosFactura_CLI> ConsultarDescuentosFactura_CLI(Expression<Func<DescuentosFactura_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<DescuentosFactura_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de DescuentosFactura_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<DescuentosFactura_CLI> ConsultarDescuentosFactura_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<DescuentosFactura_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de Retencion_TAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<Retencion_TAR> ConsultarContainsRetencion_TAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<Retencion_TAR, bool>> expresionFiltrado = ObtenerExpresionContains<Retencion_TAR>(filtro);
    			return ConsultarRetencion_TAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de Retencion_TAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Retencion_TAR> ConsultarEqualsRetencion_TAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<Retencion_TAR, bool>> expresionFiltrado = ObtenerExpresionEquals<Retencion_TAR>(filtro);
    				return ConsultarRetencion_TAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de Retencion_TAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Retencion_TAR> ConsultarRetencion_TAR(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<Retencion_TAR, bool>> expresionFiltrado = ObtenerFiltro<Retencion_TAR>(filtros,where);
    				return ConsultarRetencion_TAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de Retencion_TAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Retencion_TAR> ConsultarRetencion_TAR(Expression<Func<Retencion_TAR, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<Retencion_TAR>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de Retencion_TAR que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Retencion_TAR> ConsultarRetencion_TAR(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<Retencion_TAR>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de RetencionTipoRegimenContributivo_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<RetencionTipoRegimenContributivo_PAR> ConsultarContainsRetencionTipoRegimenContributivo_PAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<RetencionTipoRegimenContributivo_PAR, bool>> expresionFiltrado = ObtenerExpresionContains<RetencionTipoRegimenContributivo_PAR>(filtro);
    			return ConsultarRetencionTipoRegimenContributivo_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de RetencionTipoRegimenContributivo_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<RetencionTipoRegimenContributivo_PAR> ConsultarEqualsRetencionTipoRegimenContributivo_PAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<RetencionTipoRegimenContributivo_PAR, bool>> expresionFiltrado = ObtenerExpresionEquals<RetencionTipoRegimenContributivo_PAR>(filtro);
    				return ConsultarRetencionTipoRegimenContributivo_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de RetencionTipoRegimenContributivo_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<RetencionTipoRegimenContributivo_PAR> ConsultarRetencionTipoRegimenContributivo_PAR(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<RetencionTipoRegimenContributivo_PAR, bool>> expresionFiltrado = ObtenerFiltro<RetencionTipoRegimenContributivo_PAR>(filtros,where);
    				return ConsultarRetencionTipoRegimenContributivo_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de RetencionTipoRegimenContributivo_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<RetencionTipoRegimenContributivo_PAR> ConsultarRetencionTipoRegimenContributivo_PAR(Expression<Func<RetencionTipoRegimenContributivo_PAR, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<RetencionTipoRegimenContributivo_PAR>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de RetencionTipoRegimenContributivo_PAR que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<RetencionTipoRegimenContributivo_PAR> ConsultarRetencionTipoRegimenContributivo_PAR(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<RetencionTipoRegimenContributivo_PAR>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de ClienteCondicionGiro_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<ClienteCondicionGiro_CLI> ConsultarContainsClienteCondicionGiro_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<ClienteCondicionGiro_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<ClienteCondicionGiro_CLI>(filtro);
    			return ConsultarClienteCondicionGiro_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ClienteCondicionGiro_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClienteCondicionGiro_CLI> ConsultarEqualsClienteCondicionGiro_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ClienteCondicionGiro_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<ClienteCondicionGiro_CLI>(filtro);
    				return ConsultarClienteCondicionGiro_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ClienteCondicionGiro_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClienteCondicionGiro_CLI> ConsultarClienteCondicionGiro_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ClienteCondicionGiro_CLI, bool>> expresionFiltrado = ObtenerFiltro<ClienteCondicionGiro_CLI>(filtros,where);
    				return ConsultarClienteCondicionGiro_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ClienteCondicionGiro_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClienteCondicionGiro_CLI> ConsultarClienteCondicionGiro_CLI(Expression<Func<ClienteCondicionGiro_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ClienteCondicionGiro_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de ClienteCondicionGiro_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClienteCondicionGiro_CLI> ConsultarClienteCondicionGiro_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ClienteCondicionGiro_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de NotacionesFacturacionHist_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<NotacionesFacturacionHist_CLI> ConsultarContainsNotacionesFacturacionHist_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<NotacionesFacturacionHist_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<NotacionesFacturacionHist_CLI>(filtro);
    			return ConsultarNotacionesFacturacionHist_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de NotacionesFacturacionHist_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<NotacionesFacturacionHist_CLI> ConsultarEqualsNotacionesFacturacionHist_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<NotacionesFacturacionHist_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<NotacionesFacturacionHist_CLI>(filtro);
    				return ConsultarNotacionesFacturacionHist_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de NotacionesFacturacionHist_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<NotacionesFacturacionHist_CLI> ConsultarNotacionesFacturacionHist_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<NotacionesFacturacionHist_CLI, bool>> expresionFiltrado = ObtenerFiltro<NotacionesFacturacionHist_CLI>(filtros,where);
    				return ConsultarNotacionesFacturacionHist_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de NotacionesFacturacionHist_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<NotacionesFacturacionHist_CLI> ConsultarNotacionesFacturacionHist_CLI(Expression<Func<NotacionesFacturacionHist_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<NotacionesFacturacionHist_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de NotacionesFacturacionHist_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<NotacionesFacturacionHist_CLI> ConsultarNotacionesFacturacionHist_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<NotacionesFacturacionHist_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de CupoDispersionCliente_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<CupoDispersionCliente_CLI> ConsultarContainsCupoDispersionCliente_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<CupoDispersionCliente_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<CupoDispersionCliente_CLI>(filtro);
    			return ConsultarCupoDispersionCliente_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de CupoDispersionCliente_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<CupoDispersionCliente_CLI> ConsultarEqualsCupoDispersionCliente_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<CupoDispersionCliente_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<CupoDispersionCliente_CLI>(filtro);
    				return ConsultarCupoDispersionCliente_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de CupoDispersionCliente_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<CupoDispersionCliente_CLI> ConsultarCupoDispersionCliente_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<CupoDispersionCliente_CLI, bool>> expresionFiltrado = ObtenerFiltro<CupoDispersionCliente_CLI>(filtros,where);
    				return ConsultarCupoDispersionCliente_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de CupoDispersionCliente_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<CupoDispersionCliente_CLI> ConsultarCupoDispersionCliente_CLI(Expression<Func<CupoDispersionCliente_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<CupoDispersionCliente_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de CupoDispersionCliente_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<CupoDispersionCliente_CLI> ConsultarCupoDispersionCliente_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<CupoDispersionCliente_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de SuministrosSucursal_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<SuministrosSucursal_VCLI> ConsultarContainsSuministrosSucursal_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<SuministrosSucursal_VCLI, bool>> expresionFiltrado = ObtenerExpresionContains<SuministrosSucursal_VCLI>(filtro);
    			return ConsultarSuministrosSucursal_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de SuministrosSucursal_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SuministrosSucursal_VCLI> ConsultarEqualsSuministrosSucursal_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<SuministrosSucursal_VCLI, bool>> expresionFiltrado = ObtenerExpresionEquals<SuministrosSucursal_VCLI>(filtro);
    				return ConsultarSuministrosSucursal_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de SuministrosSucursal_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SuministrosSucursal_VCLI> ConsultarSuministrosSucursal_VCLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<SuministrosSucursal_VCLI, bool>> expresionFiltrado = ObtenerFiltro<SuministrosSucursal_VCLI>(filtros,where);
    				return ConsultarSuministrosSucursal_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de SuministrosSucursal_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SuministrosSucursal_VCLI> ConsultarSuministrosSucursal_VCLI(Expression<Func<SuministrosSucursal_VCLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<SuministrosSucursal_VCLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de SuministrosSucursal_VCLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SuministrosSucursal_VCLI> ConsultarSuministrosSucursal_VCLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<SuministrosSucursal_VCLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de PersonaInterna_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<PersonaInterna_PAR> ConsultarContainsPersonaInterna_PAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<PersonaInterna_PAR, bool>> expresionFiltrado = ObtenerExpresionContains<PersonaInterna_PAR>(filtro);
    			return ConsultarPersonaInterna_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de PersonaInterna_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<PersonaInterna_PAR> ConsultarEqualsPersonaInterna_PAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<PersonaInterna_PAR, bool>> expresionFiltrado = ObtenerExpresionEquals<PersonaInterna_PAR>(filtro);
    				return ConsultarPersonaInterna_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de PersonaInterna_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<PersonaInterna_PAR> ConsultarPersonaInterna_PAR(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<PersonaInterna_PAR, bool>> expresionFiltrado = ObtenerFiltro<PersonaInterna_PAR>(filtros,where);
    				return ConsultarPersonaInterna_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de PersonaInterna_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<PersonaInterna_PAR> ConsultarPersonaInterna_PAR(Expression<Func<PersonaInterna_PAR, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<PersonaInterna_PAR>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de PersonaInterna_PAR que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<PersonaInterna_PAR> ConsultarPersonaInterna_PAR(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<PersonaInterna_PAR>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de ReferenciaUsoGuia_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<ReferenciaUsoGuia_CLI> ConsultarContainsReferenciaUsoGuia_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<ReferenciaUsoGuia_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<ReferenciaUsoGuia_CLI>(filtro);
    			return ConsultarReferenciaUsoGuia_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ReferenciaUsoGuia_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ReferenciaUsoGuia_CLI> ConsultarEqualsReferenciaUsoGuia_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ReferenciaUsoGuia_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<ReferenciaUsoGuia_CLI>(filtro);
    				return ConsultarReferenciaUsoGuia_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ReferenciaUsoGuia_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ReferenciaUsoGuia_CLI> ConsultarReferenciaUsoGuia_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ReferenciaUsoGuia_CLI, bool>> expresionFiltrado = ObtenerFiltro<ReferenciaUsoGuia_CLI>(filtros,where);
    				return ConsultarReferenciaUsoGuia_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ReferenciaUsoGuia_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ReferenciaUsoGuia_CLI> ConsultarReferenciaUsoGuia_CLI(Expression<Func<ReferenciaUsoGuia_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ReferenciaUsoGuia_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de ReferenciaUsoGuia_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ReferenciaUsoGuia_CLI> ConsultarReferenciaUsoGuia_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ReferenciaUsoGuia_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de Sucursal_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<Sucursal_CLI> ConsultarContainsSucursal_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<Sucursal_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<Sucursal_CLI>(filtro);
    			return ConsultarSucursal_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de Sucursal_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Sucursal_CLI> ConsultarEqualsSucursal_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<Sucursal_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<Sucursal_CLI>(filtro);
    				return ConsultarSucursal_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de Sucursal_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Sucursal_CLI> ConsultarSucursal_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<Sucursal_CLI, bool>> expresionFiltrado = ObtenerFiltro<Sucursal_CLI>(filtros,where);
    				return ConsultarSucursal_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de Sucursal_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Sucursal_CLI> ConsultarSucursal_CLI(Expression<Func<Sucursal_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<Sucursal_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de Sucursal_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Sucursal_CLI> ConsultarSucursal_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<Sucursal_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de ReferenciaUsoGuia_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<ReferenciaUsoGuia_VCLI> ConsultarContainsReferenciaUsoGuia_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<ReferenciaUsoGuia_VCLI, bool>> expresionFiltrado = ObtenerExpresionContains<ReferenciaUsoGuia_VCLI>(filtro);
    			return ConsultarReferenciaUsoGuia_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ReferenciaUsoGuia_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ReferenciaUsoGuia_VCLI> ConsultarEqualsReferenciaUsoGuia_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ReferenciaUsoGuia_VCLI, bool>> expresionFiltrado = ObtenerExpresionEquals<ReferenciaUsoGuia_VCLI>(filtro);
    				return ConsultarReferenciaUsoGuia_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ReferenciaUsoGuia_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ReferenciaUsoGuia_VCLI> ConsultarReferenciaUsoGuia_VCLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ReferenciaUsoGuia_VCLI, bool>> expresionFiltrado = ObtenerFiltro<ReferenciaUsoGuia_VCLI>(filtros,where);
    				return ConsultarReferenciaUsoGuia_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ReferenciaUsoGuia_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ReferenciaUsoGuia_VCLI> ConsultarReferenciaUsoGuia_VCLI(Expression<Func<ReferenciaUsoGuia_VCLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ReferenciaUsoGuia_VCLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de ReferenciaUsoGuia_VCLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ReferenciaUsoGuia_VCLI> ConsultarReferenciaUsoGuia_VCLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ReferenciaUsoGuia_VCLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de ClienteSucursalContrato_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<ClienteSucursalContrato_VCLI> ConsultarContainsClienteSucursalContrato_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<ClienteSucursalContrato_VCLI, bool>> expresionFiltrado = ObtenerExpresionContains<ClienteSucursalContrato_VCLI>(filtro);
    			return ConsultarClienteSucursalContrato_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ClienteSucursalContrato_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClienteSucursalContrato_VCLI> ConsultarEqualsClienteSucursalContrato_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ClienteSucursalContrato_VCLI, bool>> expresionFiltrado = ObtenerExpresionEquals<ClienteSucursalContrato_VCLI>(filtro);
    				return ConsultarClienteSucursalContrato_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ClienteSucursalContrato_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClienteSucursalContrato_VCLI> ConsultarClienteSucursalContrato_VCLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ClienteSucursalContrato_VCLI, bool>> expresionFiltrado = ObtenerFiltro<ClienteSucursalContrato_VCLI>(filtros,where);
    				return ConsultarClienteSucursalContrato_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ClienteSucursalContrato_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClienteSucursalContrato_VCLI> ConsultarClienteSucursalContrato_VCLI(Expression<Func<ClienteSucursalContrato_VCLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ClienteSucursalContrato_VCLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de ClienteSucursalContrato_VCLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClienteSucursalContrato_VCLI> ConsultarClienteSucursalContrato_VCLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ClienteSucursalContrato_VCLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de RequisitosRadFactura_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<RequisitosRadFactura_CLI> ConsultarContainsRequisitosRadFactura_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<RequisitosRadFactura_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<RequisitosRadFactura_CLI>(filtro);
    			return ConsultarRequisitosRadFactura_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de RequisitosRadFactura_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<RequisitosRadFactura_CLI> ConsultarEqualsRequisitosRadFactura_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<RequisitosRadFactura_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<RequisitosRadFactura_CLI>(filtro);
    				return ConsultarRequisitosRadFactura_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de RequisitosRadFactura_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<RequisitosRadFactura_CLI> ConsultarRequisitosRadFactura_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<RequisitosRadFactura_CLI, bool>> expresionFiltrado = ObtenerFiltro<RequisitosRadFactura_CLI>(filtros,where);
    				return ConsultarRequisitosRadFactura_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de RequisitosRadFactura_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<RequisitosRadFactura_CLI> ConsultarRequisitosRadFactura_CLI(Expression<Func<RequisitosRadFactura_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<RequisitosRadFactura_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de RequisitosRadFactura_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<RequisitosRadFactura_CLI> ConsultarRequisitosRadFactura_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<RequisitosRadFactura_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de SucursalContraCambioEstado_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<SucursalContraCambioEstado_CLI> ConsultarContainsSucursalContraCambioEstado_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<SucursalContraCambioEstado_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<SucursalContraCambioEstado_CLI>(filtro);
    			return ConsultarSucursalContraCambioEstado_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de SucursalContraCambioEstado_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SucursalContraCambioEstado_CLI> ConsultarEqualsSucursalContraCambioEstado_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<SucursalContraCambioEstado_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<SucursalContraCambioEstado_CLI>(filtro);
    				return ConsultarSucursalContraCambioEstado_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de SucursalContraCambioEstado_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SucursalContraCambioEstado_CLI> ConsultarSucursalContraCambioEstado_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<SucursalContraCambioEstado_CLI, bool>> expresionFiltrado = ObtenerFiltro<SucursalContraCambioEstado_CLI>(filtros,where);
    				return ConsultarSucursalContraCambioEstado_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de SucursalContraCambioEstado_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SucursalContraCambioEstado_CLI> ConsultarSucursalContraCambioEstado_CLI(Expression<Func<SucursalContraCambioEstado_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<SucursalContraCambioEstado_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de SucursalContraCambioEstado_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SucursalContraCambioEstado_CLI> ConsultarSucursalContraCambioEstado_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<SucursalContraCambioEstado_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de SucursalesContrato_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<SucursalesContrato_CLI> ConsultarContainsSucursalesContrato_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<SucursalesContrato_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<SucursalesContrato_CLI>(filtro);
    			return ConsultarSucursalesContrato_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de SucursalesContrato_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SucursalesContrato_CLI> ConsultarEqualsSucursalesContrato_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<SucursalesContrato_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<SucursalesContrato_CLI>(filtro);
    				return ConsultarSucursalesContrato_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de SucursalesContrato_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SucursalesContrato_CLI> ConsultarSucursalesContrato_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<SucursalesContrato_CLI, bool>> expresionFiltrado = ObtenerFiltro<SucursalesContrato_CLI>(filtros,where);
    				return ConsultarSucursalesContrato_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de SucursalesContrato_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SucursalesContrato_CLI> ConsultarSucursalesContrato_CLI(Expression<Func<SucursalesContrato_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<SucursalesContrato_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de SucursalesContrato_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SucursalesContrato_CLI> ConsultarSucursalesContrato_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<SucursalesContrato_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de SucursalesContrato_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<SucursalesContrato_VCLI> ConsultarContainsSucursalesContrato_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<SucursalesContrato_VCLI, bool>> expresionFiltrado = ObtenerExpresionContains<SucursalesContrato_VCLI>(filtro);
    			return ConsultarSucursalesContrato_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de SucursalesContrato_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SucursalesContrato_VCLI> ConsultarEqualsSucursalesContrato_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<SucursalesContrato_VCLI, bool>> expresionFiltrado = ObtenerExpresionEquals<SucursalesContrato_VCLI>(filtro);
    				return ConsultarSucursalesContrato_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de SucursalesContrato_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SucursalesContrato_VCLI> ConsultarSucursalesContrato_VCLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<SucursalesContrato_VCLI, bool>> expresionFiltrado = ObtenerFiltro<SucursalesContrato_VCLI>(filtros,where);
    				return ConsultarSucursalesContrato_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de SucursalesContrato_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SucursalesContrato_VCLI> ConsultarSucursalesContrato_VCLI(Expression<Func<SucursalesContrato_VCLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<SucursalesContrato_VCLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de SucursalesContrato_VCLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<SucursalesContrato_VCLI> ConsultarSucursalesContrato_VCLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<SucursalesContrato_VCLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de ParametrosFactura_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<ParametrosFactura_CLI> ConsultarContainsParametrosFactura_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<ParametrosFactura_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<ParametrosFactura_CLI>(filtro);
    			return ConsultarParametrosFactura_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ParametrosFactura_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ParametrosFactura_CLI> ConsultarEqualsParametrosFactura_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ParametrosFactura_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<ParametrosFactura_CLI>(filtro);
    				return ConsultarParametrosFactura_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ParametrosFactura_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ParametrosFactura_CLI> ConsultarParametrosFactura_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ParametrosFactura_CLI, bool>> expresionFiltrado = ObtenerFiltro<ParametrosFactura_CLI>(filtros,where);
    				return ConsultarParametrosFactura_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ParametrosFactura_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ParametrosFactura_CLI> ConsultarParametrosFactura_CLI(Expression<Func<ParametrosFactura_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ParametrosFactura_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de ParametrosFactura_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ParametrosFactura_CLI> ConsultarParametrosFactura_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ParametrosFactura_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de ParametrosFacturaHist_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<ParametrosFacturaHist_CLI> ConsultarContainsParametrosFacturaHist_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<ParametrosFacturaHist_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<ParametrosFacturaHist_CLI>(filtro);
    			return ConsultarParametrosFacturaHist_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ParametrosFacturaHist_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ParametrosFacturaHist_CLI> ConsultarEqualsParametrosFacturaHist_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ParametrosFacturaHist_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<ParametrosFacturaHist_CLI>(filtro);
    				return ConsultarParametrosFacturaHist_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ParametrosFacturaHist_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ParametrosFacturaHist_CLI> ConsultarParametrosFacturaHist_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ParametrosFacturaHist_CLI, bool>> expresionFiltrado = ObtenerFiltro<ParametrosFacturaHist_CLI>(filtros,where);
    				return ConsultarParametrosFacturaHist_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ParametrosFacturaHist_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ParametrosFacturaHist_CLI> ConsultarParametrosFacturaHist_CLI(Expression<Func<ParametrosFacturaHist_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ParametrosFacturaHist_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de ParametrosFacturaHist_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ParametrosFacturaHist_CLI> ConsultarParametrosFacturaHist_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ParametrosFacturaHist_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de ContratosServicios_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<ContratosServicios_VCLI> ConsultarContainsContratosServicios_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<ContratosServicios_VCLI, bool>> expresionFiltrado = ObtenerExpresionContains<ContratosServicios_VCLI>(filtro);
    			return ConsultarContratosServicios_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ContratosServicios_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ContratosServicios_VCLI> ConsultarEqualsContratosServicios_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ContratosServicios_VCLI, bool>> expresionFiltrado = ObtenerExpresionEquals<ContratosServicios_VCLI>(filtro);
    				return ConsultarContratosServicios_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ContratosServicios_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ContratosServicios_VCLI> ConsultarContratosServicios_VCLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ContratosServicios_VCLI, bool>> expresionFiltrado = ObtenerFiltro<ContratosServicios_VCLI>(filtros,where);
    				return ConsultarContratosServicios_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ContratosServicios_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ContratosServicios_VCLI> ConsultarContratosServicios_VCLI(Expression<Func<ContratosServicios_VCLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ContratosServicios_VCLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de ContratosServicios_VCLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ContratosServicios_VCLI> ConsultarContratosServicios_VCLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ContratosServicios_VCLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de ContratoCentrosCorrespondencia_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<ContratoCentrosCorrespondencia_VCLI> ConsultarContainsContratoCentrosCorrespondencia_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<ContratoCentrosCorrespondencia_VCLI, bool>> expresionFiltrado = ObtenerExpresionContains<ContratoCentrosCorrespondencia_VCLI>(filtro);
    			return ConsultarContratoCentrosCorrespondencia_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ContratoCentrosCorrespondencia_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ContratoCentrosCorrespondencia_VCLI> ConsultarEqualsContratoCentrosCorrespondencia_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ContratoCentrosCorrespondencia_VCLI, bool>> expresionFiltrado = ObtenerExpresionEquals<ContratoCentrosCorrespondencia_VCLI>(filtro);
    				return ConsultarContratoCentrosCorrespondencia_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ContratoCentrosCorrespondencia_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ContratoCentrosCorrespondencia_VCLI> ConsultarContratoCentrosCorrespondencia_VCLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ContratoCentrosCorrespondencia_VCLI, bool>> expresionFiltrado = ObtenerFiltro<ContratoCentrosCorrespondencia_VCLI>(filtros,where);
    				return ConsultarContratoCentrosCorrespondencia_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ContratoCentrosCorrespondencia_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ContratoCentrosCorrespondencia_VCLI> ConsultarContratoCentrosCorrespondencia_VCLI(Expression<Func<ContratoCentrosCorrespondencia_VCLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ContratoCentrosCorrespondencia_VCLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de ContratoCentrosCorrespondencia_VCLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ContratoCentrosCorrespondencia_VCLI> ConsultarContratoCentrosCorrespondencia_VCLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ContratoCentrosCorrespondencia_VCLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de ContratoDescuentosFacturacion_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<ContratoDescuentosFacturacion_VCLI> ConsultarContainsContratoDescuentosFacturacion_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<ContratoDescuentosFacturacion_VCLI, bool>> expresionFiltrado = ObtenerExpresionContains<ContratoDescuentosFacturacion_VCLI>(filtro);
    			return ConsultarContratoDescuentosFacturacion_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ContratoDescuentosFacturacion_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ContratoDescuentosFacturacion_VCLI> ConsultarEqualsContratoDescuentosFacturacion_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ContratoDescuentosFacturacion_VCLI, bool>> expresionFiltrado = ObtenerExpresionEquals<ContratoDescuentosFacturacion_VCLI>(filtro);
    				return ConsultarContratoDescuentosFacturacion_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ContratoDescuentosFacturacion_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ContratoDescuentosFacturacion_VCLI> ConsultarContratoDescuentosFacturacion_VCLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ContratoDescuentosFacturacion_VCLI, bool>> expresionFiltrado = ObtenerFiltro<ContratoDescuentosFacturacion_VCLI>(filtros,where);
    				return ConsultarContratoDescuentosFacturacion_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ContratoDescuentosFacturacion_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ContratoDescuentosFacturacion_VCLI> ConsultarContratoDescuentosFacturacion_VCLI(Expression<Func<ContratoDescuentosFacturacion_VCLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ContratoDescuentosFacturacion_VCLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de ContratoDescuentosFacturacion_VCLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ContratoDescuentosFacturacion_VCLI> ConsultarContratoDescuentosFacturacion_VCLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ContratoDescuentosFacturacion_VCLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de ContratoRequisitosFacturacion_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<ContratoRequisitosFacturacion_VCLI> ConsultarContainsContratoRequisitosFacturacion_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<ContratoRequisitosFacturacion_VCLI, bool>> expresionFiltrado = ObtenerExpresionContains<ContratoRequisitosFacturacion_VCLI>(filtro);
    			return ConsultarContratoRequisitosFacturacion_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ContratoRequisitosFacturacion_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ContratoRequisitosFacturacion_VCLI> ConsultarEqualsContratoRequisitosFacturacion_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ContratoRequisitosFacturacion_VCLI, bool>> expresionFiltrado = ObtenerExpresionEquals<ContratoRequisitosFacturacion_VCLI>(filtro);
    				return ConsultarContratoRequisitosFacturacion_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ContratoRequisitosFacturacion_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ContratoRequisitosFacturacion_VCLI> ConsultarContratoRequisitosFacturacion_VCLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ContratoRequisitosFacturacion_VCLI, bool>> expresionFiltrado = ObtenerFiltro<ContratoRequisitosFacturacion_VCLI>(filtros,where);
    				return ConsultarContratoRequisitosFacturacion_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ContratoRequisitosFacturacion_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ContratoRequisitosFacturacion_VCLI> ConsultarContratoRequisitosFacturacion_VCLI(Expression<Func<ContratoRequisitosFacturacion_VCLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ContratoRequisitosFacturacion_VCLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de ContratoRequisitosFacturacion_VCLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ContratoRequisitosFacturacion_VCLI> ConsultarContratoRequisitosFacturacion_VCLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ContratoRequisitosFacturacion_VCLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de ContratoFacturacion_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<ContratoFacturacion_VCLI> ConsultarContainsContratoFacturacion_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<ContratoFacturacion_VCLI, bool>> expresionFiltrado = ObtenerExpresionContains<ContratoFacturacion_VCLI>(filtro);
    			return ConsultarContratoFacturacion_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ContratoFacturacion_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ContratoFacturacion_VCLI> ConsultarEqualsContratoFacturacion_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ContratoFacturacion_VCLI, bool>> expresionFiltrado = ObtenerExpresionEquals<ContratoFacturacion_VCLI>(filtro);
    				return ConsultarContratoFacturacion_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ContratoFacturacion_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ContratoFacturacion_VCLI> ConsultarContratoFacturacion_VCLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ContratoFacturacion_VCLI, bool>> expresionFiltrado = ObtenerFiltro<ContratoFacturacion_VCLI>(filtros,where);
    				return ConsultarContratoFacturacion_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ContratoFacturacion_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ContratoFacturacion_VCLI> ConsultarContratoFacturacion_VCLI(Expression<Func<ContratoFacturacion_VCLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ContratoFacturacion_VCLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de ContratoFacturacion_VCLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ContratoFacturacion_VCLI> ConsultarContratoFacturacion_VCLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ContratoFacturacion_VCLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de HorarioRecogidasSucursalContrato_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<HorarioRecogidasSucursalContrato_VCLI> ConsultarContainsHorarioRecogidasSucursalContrato_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<HorarioRecogidasSucursalContrato_VCLI, bool>> expresionFiltrado = ObtenerExpresionContains<HorarioRecogidasSucursalContrato_VCLI>(filtro);
    			return ConsultarHorarioRecogidasSucursalContrato_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de HorarioRecogidasSucursalContrato_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<HorarioRecogidasSucursalContrato_VCLI> ConsultarEqualsHorarioRecogidasSucursalContrato_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<HorarioRecogidasSucursalContrato_VCLI, bool>> expresionFiltrado = ObtenerExpresionEquals<HorarioRecogidasSucursalContrato_VCLI>(filtro);
    				return ConsultarHorarioRecogidasSucursalContrato_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de HorarioRecogidasSucursalContrato_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<HorarioRecogidasSucursalContrato_VCLI> ConsultarHorarioRecogidasSucursalContrato_VCLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<HorarioRecogidasSucursalContrato_VCLI, bool>> expresionFiltrado = ObtenerFiltro<HorarioRecogidasSucursalContrato_VCLI>(filtros,where);
    				return ConsultarHorarioRecogidasSucursalContrato_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de HorarioRecogidasSucursalContrato_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<HorarioRecogidasSucursalContrato_VCLI> ConsultarHorarioRecogidasSucursalContrato_VCLI(Expression<Func<HorarioRecogidasSucursalContrato_VCLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<HorarioRecogidasSucursalContrato_VCLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de HorarioRecogidasSucursalContrato_VCLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<HorarioRecogidasSucursalContrato_VCLI> ConsultarHorarioRecogidasSucursalContrato_VCLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<HorarioRecogidasSucursalContrato_VCLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de Contrato_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<Contrato_CLI> ConsultarContainsContrato_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<Contrato_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<Contrato_CLI>(filtro);
    			return ConsultarContrato_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de Contrato_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Contrato_CLI> ConsultarEqualsContrato_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<Contrato_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<Contrato_CLI>(filtro);
    				return ConsultarContrato_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de Contrato_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Contrato_CLI> ConsultarContrato_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<Contrato_CLI, bool>> expresionFiltrado = ObtenerFiltro<Contrato_CLI>(filtros,where);
    				return ConsultarContrato_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de Contrato_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Contrato_CLI> ConsultarContrato_CLI(Expression<Func<Contrato_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<Contrato_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de Contrato_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Contrato_CLI> ConsultarContrato_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<Contrato_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de ContratoHist_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<ContratoHist_CLI> ConsultarContainsContratoHist_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<ContratoHist_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<ContratoHist_CLI>(filtro);
    			return ConsultarContratoHist_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ContratoHist_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ContratoHist_CLI> ConsultarEqualsContratoHist_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ContratoHist_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<ContratoHist_CLI>(filtro);
    				return ConsultarContratoHist_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ContratoHist_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ContratoHist_CLI> ConsultarContratoHist_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ContratoHist_CLI, bool>> expresionFiltrado = ObtenerFiltro<ContratoHist_CLI>(filtros,where);
    				return ConsultarContratoHist_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ContratoHist_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ContratoHist_CLI> ConsultarContratoHist_CLI(Expression<Func<ContratoHist_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ContratoHist_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de ContratoHist_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ContratoHist_CLI> ConsultarContratoHist_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ContratoHist_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de ClienteContadoDestFrecuen_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<ClienteContadoDestFrecuen_VCLI> ConsultarContainsClienteContadoDestFrecuen_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<ClienteContadoDestFrecuen_VCLI, bool>> expresionFiltrado = ObtenerExpresionContains<ClienteContadoDestFrecuen_VCLI>(filtro);
    			return ConsultarClienteContadoDestFrecuen_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ClienteContadoDestFrecuen_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClienteContadoDestFrecuen_VCLI> ConsultarEqualsClienteContadoDestFrecuen_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ClienteContadoDestFrecuen_VCLI, bool>> expresionFiltrado = ObtenerExpresionEquals<ClienteContadoDestFrecuen_VCLI>(filtro);
    				return ConsultarClienteContadoDestFrecuen_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ClienteContadoDestFrecuen_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClienteContadoDestFrecuen_VCLI> ConsultarClienteContadoDestFrecuen_VCLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ClienteContadoDestFrecuen_VCLI, bool>> expresionFiltrado = ObtenerFiltro<ClienteContadoDestFrecuen_VCLI>(filtros,where);
    				return ConsultarClienteContadoDestFrecuen_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ClienteContadoDestFrecuen_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClienteContadoDestFrecuen_VCLI> ConsultarClienteContadoDestFrecuen_VCLI(Expression<Func<ClienteContadoDestFrecuen_VCLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ClienteContadoDestFrecuen_VCLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de ClienteContadoDestFrecuen_VCLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClienteContadoDestFrecuen_VCLI> ConsultarClienteContadoDestFrecuen_VCLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ClienteContadoDestFrecuen_VCLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de ClienteConvenioNal_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<ClienteConvenioNal_CLI> ConsultarContainsClienteConvenioNal_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<ClienteConvenioNal_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<ClienteConvenioNal_CLI>(filtro);
    			return ConsultarClienteConvenioNal_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ClienteConvenioNal_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClienteConvenioNal_CLI> ConsultarEqualsClienteConvenioNal_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ClienteConvenioNal_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<ClienteConvenioNal_CLI>(filtro);
    				return ConsultarClienteConvenioNal_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ClienteConvenioNal_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClienteConvenioNal_CLI> ConsultarClienteConvenioNal_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ClienteConvenioNal_CLI, bool>> expresionFiltrado = ObtenerFiltro<ClienteConvenioNal_CLI>(filtros,where);
    				return ConsultarClienteConvenioNal_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ClienteConvenioNal_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClienteConvenioNal_CLI> ConsultarClienteConvenioNal_CLI(Expression<Func<ClienteConvenioNal_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ClienteConvenioNal_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de ClienteConvenioNal_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClienteConvenioNal_CLI> ConsultarClienteConvenioNal_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ClienteConvenioNal_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de Localidad_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<Localidad_PAR> ConsultarContainsLocalidad_PAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<Localidad_PAR, bool>> expresionFiltrado = ObtenerExpresionContains<Localidad_PAR>(filtro);
    			return ConsultarLocalidad_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de Localidad_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Localidad_PAR> ConsultarEqualsLocalidad_PAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<Localidad_PAR, bool>> expresionFiltrado = ObtenerExpresionEquals<Localidad_PAR>(filtro);
    				return ConsultarLocalidad_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de Localidad_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Localidad_PAR> ConsultarLocalidad_PAR(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<Localidad_PAR, bool>> expresionFiltrado = ObtenerFiltro<Localidad_PAR>(filtros,where);
    				return ConsultarLocalidad_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de Localidad_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Localidad_PAR> ConsultarLocalidad_PAR(Expression<Func<Localidad_PAR, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<Localidad_PAR>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de Localidad_PAR que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<Localidad_PAR> ConsultarLocalidad_PAR(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<Localidad_PAR>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de OtroSiContrato_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<OtroSiContrato_CLI> ConsultarContainsOtroSiContrato_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<OtroSiContrato_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<OtroSiContrato_CLI>(filtro);
    			return ConsultarOtroSiContrato_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de OtroSiContrato_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<OtroSiContrato_CLI> ConsultarEqualsOtroSiContrato_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<OtroSiContrato_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<OtroSiContrato_CLI>(filtro);
    				return ConsultarOtroSiContrato_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de OtroSiContrato_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<OtroSiContrato_CLI> ConsultarOtroSiContrato_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<OtroSiContrato_CLI, bool>> expresionFiltrado = ObtenerFiltro<OtroSiContrato_CLI>(filtros,where);
    				return ConsultarOtroSiContrato_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de OtroSiContrato_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<OtroSiContrato_CLI> ConsultarOtroSiContrato_CLI(Expression<Func<OtroSiContrato_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<OtroSiContrato_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de OtroSiContrato_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<OtroSiContrato_CLI> ConsultarOtroSiContrato_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<OtroSiContrato_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de OtroSiContratoHist_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<OtroSiContratoHist_CLI> ConsultarContainsOtroSiContratoHist_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<OtroSiContratoHist_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<OtroSiContratoHist_CLI>(filtro);
    			return ConsultarOtroSiContratoHist_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de OtroSiContratoHist_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<OtroSiContratoHist_CLI> ConsultarEqualsOtroSiContratoHist_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<OtroSiContratoHist_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<OtroSiContratoHist_CLI>(filtro);
    				return ConsultarOtroSiContratoHist_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de OtroSiContratoHist_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<OtroSiContratoHist_CLI> ConsultarOtroSiContratoHist_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<OtroSiContratoHist_CLI, bool>> expresionFiltrado = ObtenerFiltro<OtroSiContratoHist_CLI>(filtros,where);
    				return ConsultarOtroSiContratoHist_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de OtroSiContratoHist_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<OtroSiContratoHist_CLI> ConsultarOtroSiContratoHist_CLI(Expression<Func<OtroSiContratoHist_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<OtroSiContratoHist_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de OtroSiContratoHist_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<OtroSiContratoHist_CLI> ConsultarOtroSiContratoHist_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<OtroSiContratoHist_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de CentroServicios_PUA que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<CentroServicios_PUA> ConsultarContainsCentroServicios_PUA(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<CentroServicios_PUA, bool>> expresionFiltrado = ObtenerExpresionContains<CentroServicios_PUA>(filtro);
    			return ConsultarCentroServicios_PUA(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de CentroServicios_PUA que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<CentroServicios_PUA> ConsultarEqualsCentroServicios_PUA(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<CentroServicios_PUA, bool>> expresionFiltrado = ObtenerExpresionEquals<CentroServicios_PUA>(filtro);
    				return ConsultarCentroServicios_PUA(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de CentroServicios_PUA que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<CentroServicios_PUA> ConsultarCentroServicios_PUA(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<CentroServicios_PUA, bool>> expresionFiltrado = ObtenerFiltro<CentroServicios_PUA>(filtros,where);
    				return ConsultarCentroServicios_PUA(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de CentroServicios_PUA que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<CentroServicios_PUA> ConsultarCentroServicios_PUA(Expression<Func<CentroServicios_PUA, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<CentroServicios_PUA>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de CentroServicios_PUA que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<CentroServicios_PUA> ConsultarCentroServicios_PUA(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<CentroServicios_PUA>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de TipoIdentificacion_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<TipoIdentificacion_PAR> ConsultarContainsTipoIdentificacion_PAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<TipoIdentificacion_PAR, bool>> expresionFiltrado = ObtenerExpresionContains<TipoIdentificacion_PAR>(filtro);
    			return ConsultarTipoIdentificacion_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de TipoIdentificacion_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<TipoIdentificacion_PAR> ConsultarEqualsTipoIdentificacion_PAR(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<TipoIdentificacion_PAR, bool>> expresionFiltrado = ObtenerExpresionEquals<TipoIdentificacion_PAR>(filtro);
    				return ConsultarTipoIdentificacion_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de TipoIdentificacion_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<TipoIdentificacion_PAR> ConsultarTipoIdentificacion_PAR(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<TipoIdentificacion_PAR, bool>> expresionFiltrado = ObtenerFiltro<TipoIdentificacion_PAR>(filtros,where);
    				return ConsultarTipoIdentificacion_PAR(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de TipoIdentificacion_PAR que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<TipoIdentificacion_PAR> ConsultarTipoIdentificacion_PAR(Expression<Func<TipoIdentificacion_PAR, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<TipoIdentificacion_PAR>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de TipoIdentificacion_PAR que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<TipoIdentificacion_PAR> ConsultarTipoIdentificacion_PAR(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<TipoIdentificacion_PAR>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de ClienteContado_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<ClienteContado_CLI> ConsultarContainsClienteContado_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<ClienteContado_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<ClienteContado_CLI>(filtro);
    			return ConsultarClienteContado_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ClienteContado_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClienteContado_CLI> ConsultarEqualsClienteContado_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ClienteContado_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<ClienteContado_CLI>(filtro);
    				return ConsultarClienteContado_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ClienteContado_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClienteContado_CLI> ConsultarClienteContado_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ClienteContado_CLI, bool>> expresionFiltrado = ObtenerFiltro<ClienteContado_CLI>(filtros,where);
    				return ConsultarClienteContado_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ClienteContado_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClienteContado_CLI> ConsultarClienteContado_CLI(Expression<Func<ClienteContado_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ClienteContado_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de ClienteContado_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClienteContado_CLI> ConsultarClienteContado_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ClienteContado_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de TipoClienteCredito_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<TipoClienteCredito_CLI> ConsultarContainsTipoClienteCredito_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<TipoClienteCredito_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<TipoClienteCredito_CLI>(filtro);
    			return ConsultarTipoClienteCredito_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de TipoClienteCredito_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<TipoClienteCredito_CLI> ConsultarEqualsTipoClienteCredito_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<TipoClienteCredito_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<TipoClienteCredito_CLI>(filtro);
    				return ConsultarTipoClienteCredito_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de TipoClienteCredito_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<TipoClienteCredito_CLI> ConsultarTipoClienteCredito_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<TipoClienteCredito_CLI, bool>> expresionFiltrado = ObtenerFiltro<TipoClienteCredito_CLI>(filtros,where);
    				return ConsultarTipoClienteCredito_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de TipoClienteCredito_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<TipoClienteCredito_CLI> ConsultarTipoClienteCredito_CLI(Expression<Func<TipoClienteCredito_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<TipoClienteCredito_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de TipoClienteCredito_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<TipoClienteCredito_CLI> ConsultarTipoClienteCredito_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<TipoClienteCredito_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de ClienteCredito_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<ClienteCredito_CLI> ConsultarContainsClienteCredito_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<ClienteCredito_CLI, bool>> expresionFiltrado = ObtenerExpresionContains<ClienteCredito_CLI>(filtro);
    			return ConsultarClienteCredito_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ClienteCredito_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClienteCredito_CLI> ConsultarEqualsClienteCredito_CLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ClienteCredito_CLI, bool>> expresionFiltrado = ObtenerExpresionEquals<ClienteCredito_CLI>(filtro);
    				return ConsultarClienteCredito_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ClienteCredito_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClienteCredito_CLI> ConsultarClienteCredito_CLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ClienteCredito_CLI, bool>> expresionFiltrado = ObtenerFiltro<ClienteCredito_CLI>(filtros,where);
    				return ConsultarClienteCredito_CLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ClienteCredito_CLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClienteCredito_CLI> ConsultarClienteCredito_CLI(Expression<Func<ClienteCredito_CLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ClienteCredito_CLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de ClienteCredito_CLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClienteCredito_CLI> ConsultarClienteCredito_CLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ClienteCredito_CLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    		/// <summary>
    		/// Retorna la lista de ClientesCreditoLocalidad_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    			public IEnumerable<ClientesCreditoLocalidad_VCLI> ConsultarContainsClientesCreditoLocalidad_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			Expression<Func<ClientesCreditoLocalidad_VCLI, bool>> expresionFiltrado = ObtenerExpresionContains<ClientesCreditoLocalidad_VCLI>(filtro);
    			return ConsultarClientesCreditoLocalidad_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ClientesCreditoLocalidad_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClientesCreditoLocalidad_VCLI> ConsultarEqualsClientesCreditoLocalidad_VCLI(IDictionary<string, string> filtro, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ClientesCreditoLocalidad_VCLI, bool>> expresionFiltrado = ObtenerExpresionEquals<ClientesCreditoLocalidad_VCLI>(filtro);
    				return ConsultarClientesCreditoLocalidad_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ClientesCreditoLocalidad_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="filtro">Diccionario a través del cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClientesCreditoLocalidad_VCLI> ConsultarClientesCreditoLocalidad_VCLI(IDictionary<string, string> filtros, Dictionary<LambdaExpression, OperadorLogico> where, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    				Expression<Func<ClientesCreditoLocalidad_VCLI, bool>> expresionFiltrado = ObtenerFiltro<ClientesCreditoLocalidad_VCLI>(filtros,where);
    				return ConsultarClientesCreditoLocalidad_VCLI(expresionFiltrado, campoOrdenamiento, out totalRegistros, indicePagina, registrosPorPagina, esAscendente);
    		}
    
    		/// <summary>
    		/// Retorna la lista de ClientesCreditoLocalidad_VCLI que cumpla con las expresiones de filtrado y ordenamiento
          /// </summary>
    		/// <param name="expresionFiltrado">Expresión por la cual se filtra la lista</param>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de de registros que satisfacen la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClientesCreditoLocalidad_VCLI> ConsultarClientesCreditoLocalidad_VCLI(Expression<Func<ClientesCreditoLocalidad_VCLI, bool>> expresionFiltrado, string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Número de Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query			
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ClientesCreditoLocalidad_VCLI>();
    
    				   totalRegistros = objectSet.Where(expresionFiltrado).Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
    													  .Where(expresionFiltrado)										
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" ASC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList()
        											 :
        									objectSet.OrdenarUsandoExpresion(campoOrdenamiento+" DESC")
        													.Where(expresionFiltrado)										
        													.Skip(indicePagina * registrosPorPagina)
        													.Take(registrosPorPagina)
        													.ToList();
    			}	
    		}
    
    		/// <summary>
          /// Retorna la lista de ClientesCreditoLocalidad_VCLI que cumpla con la expresión de ordenamiento
          /// </summary>
        	/// <param name="campoOrdenamiento">Campo por el cual se va a realizar el ordenamiento</param>
    		/// <param name="totalRegistros">Retorna el total de registros que genera la consulta sin paginar</param>
        	/// <param name="indicePagina">Índice de página a retornar</param>
        	/// <param name="registrosPorPagina">Registros por página</param>
        	/// <param name="esAscendente">Indica si es ascendente el ordenamiento</param>
        	/// <returns></returns>
    		public IEnumerable<ClientesCreditoLocalidad_VCLI> ConsultarClientesCreditoLocalidad_VCLI(string campoOrdenamiento, out int totalRegistros, int indicePagina = 0, int registrosPorPagina = 20, bool esAscendente = true)
    		{
    			// Verificando parámetros para la consulta
    			if (indicePagina < 0)
    				throw new ArgumentException("Página inválida");
    
    			if (registrosPorPagina <= 0)
    				throw new ArgumentException("Número de registros por página no válido");
    
    			//Create associated IObjectSet and perform query
    			var objectSet = ((IObjectContextAdapter)this).ObjectContext.CreateObjectSet<ClientesCreditoLocalidad_VCLI>();
    		
    		   totalRegistros = objectSet.Count();
             
    			if (campoOrdenamiento.Trim() == string.Empty)
             {
                string llavePrimaria = objectSet.EntitySet.ElementType.KeyMembers.First().Name;
                return objectSet.OrdenarPor(llavePrimaria)
                                             .Skip(indicePagina * registrosPorPagina)
                                             .Take(registrosPorPagina).ToList();
             }
    			else
    			{
    				return (esAscendente) ? objectSet.OrdenarPor(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList()
    											 :
    									objectSet.OrdenarPorDescendente(campoOrdenamiento)
    													.Skip(indicePagina * registrosPorPagina)
    													.Take(registrosPorPagina)
    													.ToList();
    			}
    		}
    	
        #endregion

    
    		#region Propiedades
        public DbSet<TipoActividadEconomica_PAR> TipoActividadEconomica_PAR { get; set; }
        public DbSet<TipoRegimenContributivo_PAR> TipoRegimenContributivo_PAR { get; set; }
        public DbSet<TipoSegmentoMercado_PAR> TipoSegmentoMercado_PAR { get; set; }
        public DbSet<TipoSociedad_PAR> TipoSociedad_PAR { get; set; }
        public DbSet<Localidades_VPAR> Localidades_VPAR { get; set; }
        public DbSet<MotivoEstados_PAR> MotivoEstados_PAR { get; set; }
        public DbSet<PersonaResponsableLegal_PAR> PersonaResponsableLegal_PAR { get; set; }
        public DbSet<PersonaExterna_PAR> PersonaExterna_PAR { get; set; }
        public DbSet<DocumentosClienteCredito_VCLI> DocumentosClienteCredito_VCLI { get; set; }
        public DbSet<AcumuladoGirosCliente_GIR> AcumuladoGirosCliente_GIR { get; set; }
        public DbSet<Ocupacion_PAR> Ocupacion_PAR { get; set; }
        public DbSet<DocumentosClienteCredito_CLI> DocumentosClienteCredito_CLI { get; set; }
        public DbSet<DestinatarioFrecuente_CLI> DestinatarioFrecuente_CLI { get; set; }
        public DbSet<PersonalContrato_CLI> PersonalContrato_CLI { get; set; }
        public DbSet<Cargo_SEG> Cargo_SEG { get; set; }
        public DbSet<PersonaInternaCargoCenSer_VPAR> PersonaInternaCargoCenSer_VPAR { get; set; }
        public DbSet<ModalidadOtroSi_CLI> ModalidadOtroSi_CLI { get; set; }
        public DbSet<ClienteContadoHist_CLI> ClienteContadoHist_CLI { get; set; }
        public DbSet<DocumentosContrato_CLI> DocumentosContrato_CLI { get; set; }
        public DbSet<HorarioRecogidaSucursalContrato_CLI> HorarioRecogidaSucursalContrato_CLI { get; set; }
        public DbSet<SucursalesContratoHist_CLI> SucursalesContratoHist_CLI { get; set; }
        public DbSet<Zona_PAR> Zona_PAR { get; set; }
        public DbSet<ZonaDeLocalidad_PAR> ZonaDeLocalidad_PAR { get; set; }
        public DbSet<TipoNotacionFacturacion_CLI> TipoNotacionFacturacion_CLI { get; set; }
        public DbSet<NotacionesFacturacion_CLI> NotacionesFacturacion_CLI { get; set; }
        public DbSet<AgrupacionFactura_CLI> AgrupacionFactura_CLI { get; set; }
        public DbSet<Servicio_TAR> Servicio_TAR { get; set; }
        public DbSet<AgrupacionFacturaSvcSucur_VCLI> AgrupacionFacturaSvcSucur_VCLI { get; set; }
        public DbSet<VersionServicios_VVER> VersionServicios_VVER { get; set; }
        public DbSet<AcumuladosContrato_CLI> AcumuladosContrato_CLI { get; set; }
        public DbSet<CliContadoArchivoCedulas_VCLI> CliContadoArchivoCedulas_VCLI { get; set; }
        public DbSet<ListaPrecios_TAR> ListaPrecios_TAR { get; set; }
        public DbSet<SucursalServicios_VCLI> SucursalServicios_VCLI { get; set; }
        public DbSet<SucursalesAgencia_VCLI> SucursalesAgencia_VCLI { get; set; }
        public DbSet<SucursalesClienteRacol_VCLI> SucursalesClienteRacol_VCLI { get; set; }
        public DbSet<EstadosCliente_CLI> EstadosCliente_CLI { get; set; }
        public DbSet<ClienteCreditoHist_CLI> ClienteCreditoHist_CLI { get; set; }
        public DbSet<SucursalHist_CLI> SucursalHist_CLI { get; set; }
        public DbSet<ContactosContrato_CLI> ContactosContrato_CLI { get; set; }
        public DbSet<DeduccionesFacturacion_CLI> DeduccionesFacturacion_CLI { get; set; }
        public DbSet<DescuentosFactura_CLI> DescuentosFactura_CLI { get; set; }
        public DbSet<Retencion_TAR> Retencion_TAR { get; set; }
        public DbSet<RetencionTipoRegimenContributivo_PAR> RetencionTipoRegimenContributivo_PAR { get; set; }
        public DbSet<ClienteCondicionGiro_CLI> ClienteCondicionGiro_CLI { get; set; }
        public DbSet<NotacionesFacturacionHist_CLI> NotacionesFacturacionHist_CLI { get; set; }
        public DbSet<CupoDispersionCliente_CLI> CupoDispersionCliente_CLI { get; set; }
        public DbSet<SuministrosSucursal_VCLI> SuministrosSucursal_VCLI { get; set; }
        public DbSet<PersonaInterna_PAR> PersonaInterna_PAR { get; set; }
        public DbSet<ReferenciaUsoGuia_CLI> ReferenciaUsoGuia_CLI { get; set; }
        public DbSet<Sucursal_CLI> Sucursal_CLI { get; set; }
        public DbSet<ReferenciaUsoGuia_VCLI> ReferenciaUsoGuia_VCLI { get; set; }
        public DbSet<ClienteSucursalContrato_VCLI> ClienteSucursalContrato_VCLI { get; set; }
        public DbSet<RequisitosRadFactura_CLI> RequisitosRadFactura_CLI { get; set; }
        public DbSet<SucursalContraCambioEstado_CLI> SucursalContraCambioEstado_CLI { get; set; }
        public DbSet<SucursalesContrato_CLI> SucursalesContrato_CLI { get; set; }
        public DbSet<SucursalesContrato_VCLI> SucursalesContrato_VCLI { get; set; }
        public DbSet<ParametrosFactura_CLI> ParametrosFactura_CLI { get; set; }
        public DbSet<ParametrosFacturaHist_CLI> ParametrosFacturaHist_CLI { get; set; }
        public DbSet<ContratosServicios_VCLI> ContratosServicios_VCLI { get; set; }
        public DbSet<ContratoCentrosCorrespondencia_VCLI> ContratoCentrosCorrespondencia_VCLI { get; set; }
        public DbSet<ContratoDescuentosFacturacion_VCLI> ContratoDescuentosFacturacion_VCLI { get; set; }
        public DbSet<ContratoRequisitosFacturacion_VCLI> ContratoRequisitosFacturacion_VCLI { get; set; }
        public DbSet<ContratoFacturacion_VCLI> ContratoFacturacion_VCLI { get; set; }
        public DbSet<HorarioRecogidasSucursalContrato_VCLI> HorarioRecogidasSucursalContrato_VCLI { get; set; }
        public DbSet<Contrato_CLI> Contrato_CLI { get; set; }
        public DbSet<ContratoHist_CLI> ContratoHist_CLI { get; set; }
        public DbSet<ClienteContadoDestFrecuen_VCLI> ClienteContadoDestFrecuen_VCLI { get; set; }
        public DbSet<ClienteConvenioNal_CLI> ClienteConvenioNal_CLI { get; set; }
        public DbSet<Localidad_PAR> Localidad_PAR { get; set; }
        public DbSet<OtroSiContrato_CLI> OtroSiContrato_CLI { get; set; }
        public DbSet<OtroSiContratoHist_CLI> OtroSiContratoHist_CLI { get; set; }
        public DbSet<CentroServicios_PUA> CentroServicios_PUA { get; set; }
        public DbSet<TipoIdentificacion_PAR> TipoIdentificacion_PAR { get; set; }
        public DbSet<ClienteContado_CLI> ClienteContado_CLI { get; set; }
        public DbSet<TipoClienteCredito_CLI> TipoClienteCredito_CLI { get; set; }
        public DbSet<ClienteCredito_CLI> ClienteCredito_CLI { get; set; }
        public DbSet<ClientesCreditoLocalidad_VCLI> ClientesCreditoLocalidad_VCLI { get; set; }
    
        public virtual ObjectResult<CLpaObtenerSucursalSuminist> paObtenerSucursalSuminist_CLI(Nullable<int> sUCURSAL, Nullable<int> sUMINISTRO)
        {
            ((IObjectContextAdapter)this).ObjectContext.MetadataWorkspace.LoadFromAssembly(typeof(CLpaObtenerSucursalSuminist).Assembly);
    
            var sUCURSALParameter = sUCURSAL.HasValue ?
                new ObjectParameter("SUCURSAL", sUCURSAL) :
                new ObjectParameter("SUCURSAL", typeof(int));
    
            var sUMINISTROParameter = sUMINISTRO.HasValue ?
                new ObjectParameter("SUMINISTRO", sUMINISTRO) :
                new ObjectParameter("SUMINISTRO", typeof(int));
    
            return ((IObjectContextAdapter)this).ObjectContext.ExecuteFunction<CLpaObtenerSucursalSuminist>("paObtenerSucursalSuminist_CLI", sUCURSALParameter, sUMINISTROParameter);
        }
    
        public virtual ObjectResult<CLClienteContado_CLI> paObtenerClienteContado_CLI(string identificacion, string tipoId, string telefono)
        {
            ((IObjectContextAdapter)this).ObjectContext.MetadataWorkspace.LoadFromAssembly(typeof(CLClienteContado_CLI).Assembly);
    
            var identificacionParameter = identificacion != null ?
                new ObjectParameter("identificacion", identificacion) :
                new ObjectParameter("identificacion", typeof(string));
    
            var tipoIdParameter = tipoId != null ?
                new ObjectParameter("tipoId", tipoId) :
                new ObjectParameter("tipoId", typeof(string));
    
            var telefonoParameter = telefono != null ?
                new ObjectParameter("telefono", telefono) :
                new ObjectParameter("telefono", typeof(string));
    
            return ((IObjectContextAdapter)this).ObjectContext.ExecuteFunction<CLClienteContado_CLI>("paObtenerClienteContado_CLI", identificacionParameter, tipoIdParameter, telefonoParameter);
        }
    
        public virtual ObjectResult<paObtenerSucContrato_CLI> paObtenerSucContrato_CLI(Nullable<int> pageIndex, Nullable<int> pageSize, Nullable<int> idCliente, Nullable<int> idContrato, Nullable<int> idSucursal, string nombre)
        {
            ((IObjectContextAdapter)this).ObjectContext.MetadataWorkspace.LoadFromAssembly(typeof(paObtenerSucContrato_CLI).Assembly);
    
            var pageIndexParameter = pageIndex.HasValue ?
                new ObjectParameter("PageIndex", pageIndex) :
                new ObjectParameter("PageIndex", typeof(int));
    
            var pageSizeParameter = pageSize.HasValue ?
                new ObjectParameter("PageSize", pageSize) :
                new ObjectParameter("PageSize", typeof(int));
    
            var idClienteParameter = idCliente.HasValue ?
                new ObjectParameter("idCliente", idCliente) :
                new ObjectParameter("idCliente", typeof(int));
    
            var idContratoParameter = idContrato.HasValue ?
                new ObjectParameter("idContrato", idContrato) :
                new ObjectParameter("idContrato", typeof(int));
    
            var idSucursalParameter = idSucursal.HasValue ?
                new ObjectParameter("idSucursal", idSucursal) :
                new ObjectParameter("idSucursal", typeof(int));
    
            var nombreParameter = nombre != null ?
                new ObjectParameter("nombre", nombre) :
                new ObjectParameter("nombre", typeof(string));
    
            return ((IObjectContextAdapter)this).ObjectContext.ExecuteFunction<paObtenerSucContrato_CLI>("paObtenerSucContrato_CLI", pageIndexParameter, pageSizeParameter, idClienteParameter, idContratoParameter, idSucursalParameter, nombreParameter);
        }
    
        public virtual ObjectResult<CLpaObtenerCliCreditoContratCondicionGiro> paObtenerCliCreditoContratCondicionGiro_CLI(Nullable<bool> permiteGiroConvenio, Nullable<bool> permiteDispersion)
        {
            ((IObjectContextAdapter)this).ObjectContext.MetadataWorkspace.LoadFromAssembly(typeof(CLpaObtenerCliCreditoContratCondicionGiro).Assembly);
    
            var permiteGiroConvenioParameter = permiteGiroConvenio.HasValue ?
                new ObjectParameter("permiteGiroConvenio", permiteGiroConvenio) :
                new ObjectParameter("permiteGiroConvenio", typeof(bool));
    
            var permiteDispersionParameter = permiteDispersion.HasValue ?
                new ObjectParameter("permiteDispersion", permiteDispersion) :
                new ObjectParameter("permiteDispersion", typeof(bool));
    
            return ((IObjectContextAdapter)this).ObjectContext.ExecuteFunction<CLpaObtenerCliCreditoContratCondicionGiro>("paObtenerCliCreditoContratCondicionGiro_CLI", permiteGiroConvenioParameter, permiteDispersionParameter);
        }
    
        public virtual ObjectResult<CLpaObtenerCliCreditoDispersionFondosGiro> paObtenerCliCreditoDispersionFondosGiro_CLI()
        {
            ((IObjectContextAdapter)this).ObjectContext.MetadataWorkspace.LoadFromAssembly(typeof(CLpaObtenerCliCreditoDispersionFondosGiro).Assembly);
    
            return ((IObjectContextAdapter)this).ObjectContext.ExecuteFunction<CLpaObtenerCliCreditoDispersionFondosGiro>("paObtenerCliCreditoDispersionFondosGiro_CLI");
        }
    
        public virtual ObjectResult<Nullable<int>> paObtenerSucursalSumSerial_CLI(Nullable<long> sERIAL, Nullable<int> sUMINISTRO)
        {
            var sERIALParameter = sERIAL.HasValue ?
                new ObjectParameter("SERIAL", sERIAL) :
                new ObjectParameter("SERIAL", typeof(long));
    
            var sUMINISTROParameter = sUMINISTRO.HasValue ?
                new ObjectParameter("SUMINISTRO", sUMINISTRO) :
                new ObjectParameter("SUMINISTRO", typeof(int));
    
            return ((IObjectContextAdapter)this).ObjectContext.ExecuteFunction<Nullable<int>>("paObtenerSucursalSumSerial_CLI", sERIALParameter, sUMINISTROParameter);
        }
    
        public virtual ObjectResult<paObtenerSucursalesFacExcluidas_CLI> paObtenerSucursalesFacExcluidas_CLI(Nullable<int> pageIndex, Nullable<int> pageSize, Nullable<int> idContrato, Nullable<int> idFactura, Nullable<int> idServicio, Nullable<int> idSucursal, string nombreSucursal)
        {
            ((IObjectContextAdapter)this).ObjectContext.MetadataWorkspace.LoadFromAssembly(typeof(paObtenerSucursalesFacExcluidas_CLI).Assembly);
    
            var pageIndexParameter = pageIndex.HasValue ?
                new ObjectParameter("PageIndex", pageIndex) :
                new ObjectParameter("PageIndex", typeof(int));
    
            var pageSizeParameter = pageSize.HasValue ?
                new ObjectParameter("PageSize", pageSize) :
                new ObjectParameter("PageSize", typeof(int));
    
            var idContratoParameter = idContrato.HasValue ?
                new ObjectParameter("idContrato", idContrato) :
                new ObjectParameter("idContrato", typeof(int));
    
            var idFacturaParameter = idFactura.HasValue ?
                new ObjectParameter("idFactura", idFactura) :
                new ObjectParameter("idFactura", typeof(int));
    
            var idServicioParameter = idServicio.HasValue ?
                new ObjectParameter("idServicio", idServicio) :
                new ObjectParameter("idServicio", typeof(int));
    
            var idSucursalParameter = idSucursal.HasValue ?
                new ObjectParameter("idSucursal", idSucursal) :
                new ObjectParameter("idSucursal", typeof(int));
    
            var nombreSucursalParameter = nombreSucursal != null ?
                new ObjectParameter("nombreSucursal", nombreSucursal) :
                new ObjectParameter("nombreSucursal", typeof(string));
    
            return ((IObjectContextAdapter)this).ObjectContext.ExecuteFunction<paObtenerSucursalesFacExcluidas_CLI>("paObtenerSucursalesFacExcluidas_CLI", pageIndexParameter, pageSizeParameter, idContratoParameter, idFacturaParameter, idServicioParameter, idSucursalParameter, nombreSucursalParameter);
        }
    
        public virtual ObjectResult<Nullable<decimal>> paObtenerConsumoPeriodo_CLI(Nullable<int> aCC_IdContrato)
        {
            var aCC_IdContratoParameter = aCC_IdContrato.HasValue ?
                new ObjectParameter("ACC_IdContrato", aCC_IdContrato) :
                new ObjectParameter("ACC_IdContrato", typeof(int));
    
            return ((IObjectContextAdapter)this).ObjectContext.ExecuteFunction<Nullable<decimal>>("paObtenerConsumoPeriodo_CLI", aCC_IdContratoParameter);
        }
    
        public virtual int paGestionarAcumuladoContrato_CLI(Nullable<int> cON_IdContrato, Nullable<decimal> aCC_ValorTransaccion, string aCC_CreadoPor)
        {
            var cON_IdContratoParameter = cON_IdContrato.HasValue ?
                new ObjectParameter("CON_IdContrato", cON_IdContrato) :
                new ObjectParameter("CON_IdContrato", typeof(int));
    
            var aCC_ValorTransaccionParameter = aCC_ValorTransaccion.HasValue ?
                new ObjectParameter("ACC_ValorTransaccion", aCC_ValorTransaccion) :
                new ObjectParameter("ACC_ValorTransaccion", typeof(decimal));
    
            var aCC_CreadoPorParameter = aCC_CreadoPor != null ?
                new ObjectParameter("ACC_CreadoPor", aCC_CreadoPor) :
                new ObjectParameter("ACC_CreadoPor", typeof(string));
    
            return ((IObjectContextAdapter)this).ObjectContext.ExecuteFunction("paGestionarAcumuladoContrato_CLI", cON_IdContratoParameter, aCC_ValorTransaccionParameter, aCC_CreadoPorParameter);
        }
    
        public virtual ObjectResult<paObtenerCliContratoAgen_CLI_Result> paObtenerCliContratoAgen_CLI(Nullable<long> idAgencia)
        {
            ((IObjectContextAdapter)this).ObjectContext.MetadataWorkspace.LoadFromAssembly(typeof(paObtenerCliContratoAgen_CLI_Result).Assembly);
    
            var idAgenciaParameter = idAgencia.HasValue ?
                new ObjectParameter("IdAgencia", idAgencia) :
                new ObjectParameter("IdAgencia", typeof(long));
    
            return ((IObjectContextAdapter)this).ObjectContext.ExecuteFunction<paObtenerCliContratoAgen_CLI_Result>("paObtenerCliContratoAgen_CLI", idAgenciaParameter);
        }
    
        public virtual ObjectResult<paObtenerServActPorSucLisPrecio_CLI_Result> paObtenerServActPorSucLisPrecio_CLI(Nullable<int> idSucursal, Nullable<int> idListaPrecios, string idUnidadMensajeria, string idUnidadCarga)
        {
            ((IObjectContextAdapter)this).ObjectContext.MetadataWorkspace.LoadFromAssembly(typeof(paObtenerServActPorSucLisPrecio_CLI_Result).Assembly);
    
            var idSucursalParameter = idSucursal.HasValue ?
                new ObjectParameter("IdSucursal", idSucursal) :
                new ObjectParameter("IdSucursal", typeof(int));
    
            var idListaPreciosParameter = idListaPrecios.HasValue ?
                new ObjectParameter("IdListaPrecios", idListaPrecios) :
                new ObjectParameter("IdListaPrecios", typeof(int));
    
            var idUnidadMensajeriaParameter = idUnidadMensajeria != null ?
                new ObjectParameter("IdUnidadMensajeria", idUnidadMensajeria) :
                new ObjectParameter("IdUnidadMensajeria", typeof(string));
    
            var idUnidadCargaParameter = idUnidadCarga != null ?
                new ObjectParameter("IdUnidadCarga", idUnidadCarga) :
                new ObjectParameter("IdUnidadCarga", typeof(string));
    
            return ((IObjectContextAdapter)this).ObjectContext.ExecuteFunction<paObtenerServActPorSucLisPrecio_CLI_Result>("paObtenerServActPorSucLisPrecio_CLI", idSucursalParameter, idListaPreciosParameter, idUnidadMensajeriaParameter, idUnidadCargaParameter);
        }
    
        public virtual ObjectResult<paObtenerServActPorSuc_CLI_Result> paObtenerServActPorSuc_CLI(Nullable<int> idSucursal, string idUnidadMensajeria, string idUnidadCarga)
        {
            ((IObjectContextAdapter)this).ObjectContext.MetadataWorkspace.LoadFromAssembly(typeof(paObtenerServActPorSuc_CLI_Result).Assembly);
    
            var idSucursalParameter = idSucursal.HasValue ?
                new ObjectParameter("IdSucursal", idSucursal) :
                new ObjectParameter("IdSucursal", typeof(int));
    
            var idUnidadMensajeriaParameter = idUnidadMensajeria != null ?
                new ObjectParameter("IdUnidadMensajeria", idUnidadMensajeria) :
                new ObjectParameter("IdUnidadMensajeria", typeof(string));
    
            var idUnidadCargaParameter = idUnidadCarga != null ?
                new ObjectParameter("IdUnidadCarga", idUnidadCarga) :
                new ObjectParameter("IdUnidadCarga", typeof(string));
    
            return ((IObjectContextAdapter)this).ObjectContext.ExecuteFunction<paObtenerServActPorSuc_CLI_Result>("paObtenerServActPorSuc_CLI", idSucursalParameter, idUnidadMensajeriaParameter, idUnidadCargaParameter);
        }
    
        public virtual ObjectResult<paObtenerClientesConvenioNal_CLI_Result> paObtenerClientesConvenioNal_CLI1(string idLocalidadOrigen, string idLocalidadDestino)
        {
            ((IObjectContextAdapter)this).ObjectContext.MetadataWorkspace.LoadFromAssembly(typeof(paObtenerClientesConvenioNal_CLI_Result).Assembly);
    
            var idLocalidadOrigenParameter = idLocalidadOrigen != null ?
                new ObjectParameter("IdLocalidadOrigen", idLocalidadOrigen) :
                new ObjectParameter("IdLocalidadOrigen", typeof(string));
    
            var idLocalidadDestinoParameter = idLocalidadDestino != null ?
                new ObjectParameter("IdLocalidadDestino", idLocalidadDestino) :
                new ObjectParameter("IdLocalidadDestino", typeof(string));
    
            return ((IObjectContextAdapter)this).ObjectContext.ExecuteFunction<paObtenerClientesConvenioNal_CLI_Result>("paObtenerClientesConvenioNal_CLI1", idLocalidadOrigenParameter, idLocalidadDestinoParameter);
        }
    
        public virtual int PaValidarCupoContrato_CLI(Nullable<int> aCC_IdContrato, Nullable<long> valorTransaccion, ObjectParameter resultado)
        {
            var aCC_IdContratoParameter = aCC_IdContrato.HasValue ?
                new ObjectParameter("ACC_IdContrato", aCC_IdContrato) :
                new ObjectParameter("ACC_IdContrato", typeof(int));
    
            var valorTransaccionParameter = valorTransaccion.HasValue ?
                new ObjectParameter("valorTransaccion", valorTransaccion) :
                new ObjectParameter("valorTransaccion", typeof(long));
    
            return ((IObjectContextAdapter)this).ObjectContext.ExecuteFunction("PaValidarCupoContrato_CLI", aCC_IdContratoParameter, valorTransaccionParameter, resultado);
        }
    	#endregion
    }
		/// <summary>
    /// Operadores de comparación en una sentencia where
    /// </summary>
    public enum OperadorComparacion
    {
        Equal,
        NotEqual,
        Contains,
        Between
    }

    /// <summary>
    /// Operadores lógicos en una sentencia where
    /// </summary>
    public enum OperadorLogico
    {
        Or,
        And
    }    

		  public class ParameterRebinder : ExpressionVisitor
  {
    private readonly Dictionary<ParameterExpression, ParameterExpression> map;

    public ParameterRebinder(Dictionary<ParameterExpression, ParameterExpression> map)
    {
      this.map = map ?? new Dictionary<ParameterExpression, ParameterExpression>();
    }

    public static Expression ReplaceParameters(Dictionary<ParameterExpression, ParameterExpression> map, Expression exp)
    {
      return new ParameterRebinder(map).Visit(exp);
    }

    protected override Expression VisitParameter(ParameterExpression p)
    {
      ParameterExpression replacement;
      if (map.TryGetValue(p, out replacement))
      {
        p = replacement;
      }
      return base.VisitParameter(p);
    }
  }

  public static class Utility
  {
    public static Expression<T> Compose<T>(this Expression<T> first, Expression<T> second, Func<Expression, Expression, Expression> merge)
    {
      // build parameter map (from parameters of second to parameters of first)
      var map = first.Parameters.Select((f, i) => new { f, s = second.Parameters[i] }).ToDictionary(p => p.s, p => p.f);

      // replace parameters in the second lambda expression with parameters from the first
      var secondBody = ParameterRebinder.ReplaceParameters(map, second.Body);

      // apply composition of lambda expression bodies to parameters from the first expression
      return Expression.Lambda<T>(merge(first.Body, secondBody), first.Parameters);
    }

    public static Expression<Func<T, bool>> And<T>(this Expression<Func<T, bool>> first, Expression<Func<T, bool>> second)
    {
      return first.Compose(second, Expression.And);
    }

    public static Expression<Func<T, bool>> Or<T>(this Expression<Func<T, bool>> first, Expression<Func<T, bool>> second)
    {
      return first.Compose(second, Expression.Or);
    }
  }

	  public static class AuditExtensions
    {
      /// <summary>
      /// Adds Auditing to an ObjectContext for the specified <paramref name="fromType"/>.
      /// </summary>
      /// <typeparam name="fromType">The source entity type (including derivatives) to audit changes to.</typeparam>
      /// <typeparam name="toType">The target entity type that audit records will be stored in.</typeparam>
      /// <param name="context">The ObjectContext for which Auditing should be enabled.</param>
      /// <param name="mapping">A lambda expression that maps the changed <paramref name="fromType"/> entity to a <paramref name="toType"/> audit record.</param>
      /// <param name="addToContext">A lambda expression that adds the audit entity to an entity collection.</param>
      public static void Audit<fromType, toType>(
          this DbContext context,
          Func<IDataRecord, EntityState, toType> mapping,
          Action<toType> addToContext)
      {
        IObjectContextAdapter oc = context as IObjectContextAdapter;
        oc.ObjectContext.SavingChanges +=
            new EventHandler((o, e) =>
                CreateAuditRecord<fromType, toType>(context, mapping, addToContext));
      }

      /// <summary>
      /// Helper function that is called during ObjectContext.SavingChanges, and is responsible for managing the
      /// process of mapping all changed entities to an audit entity, and adding them to an entity collection.
      /// </summary>
      /// <remarks>
      /// Note that at present this function will be called once for every entity type that is being audited. If
      /// you don't like this it should be easy to optimize it.
      /// </remarks>
      /// <typeparam name="fromType">The source entity type (including derivatives) to audit changes to.</typeparam>
      /// <typeparam name="toType">The target entity type that audit records will be stored in.</typeparam>
      /// <param name="context">The ObjectContext for which Auditing should be enabled.</param>
      /// <param name="mapping">A lambda expression that maps the changed <paramref name="fromType"/> entity to a <paramref name="toType"/> audit record.</param>
      /// <param name="addToContext">A lambda expression that adds the audit entity to an entity collection.</param>
      private static void CreateAuditRecord<fromType, toType>(
          DbContext context,
          Func<IDataRecord, EntityState, toType> mapping,
          Action<toType> addToContext)
      {
        IObjectContextAdapter oc = context as IObjectContextAdapter;
        oc.ObjectContext.DetectChanges();
        // get a list of changes to audit
        IEnumerable<ObjectStateEntry> entities =
            from e in oc.ObjectContext.ObjectStateManager.GetObjectStateEntries(
                EntityState.Modified | EntityState.Deleted)
            where
                e.IsRelationship == false &&
                typeof(fromType).IsAssignableFrom(e.Entity.GetType())
            select e;

        foreach (ObjectStateEntry item in entities)
        {
          // map the changed item to an audit record entry
          toType auditRecord = mapping(item.OriginalValues, item.State);
          // and add it to the object context so that it is persisted for us
          addToContext(auditRecord);
        }
      }

      /// <summary>
      /// Retrieves a fields value from an IDataRecord (including a DbDataRecord) in a strongly typed,
      /// compile-time checked fashion. This overload assumes the IDataRecord represents an EntityObject
      /// for which a property's value should be retrieved.
      /// </summary>
      /// <typeparam name="E">The Entity type the IDataRecord represents</typeparam>
      /// <typeparam name="T">The data type of the field</typeparam>
      /// <param name="record">The IDataRecord that contains the data</param>
      /// <param name="propertySelector">An expression that identifies the entity member to retrieve</param>
      /// <returns>The field's value.</returns>
      /// <example>
      /// <code>
      /// IDataRecord record = GetRecord();
      /// string name = record.Field&lt;Person, string>(person => person.Name);
      /// int age = record.Field&lt;Person, int>(person => person.Age);
      /// DateTime createdDate = record.Field&lt;Person, DateTime>(person => person.CreatedDate);
      /// </code>
      /// </example>
      public static T Field<E, T>(this IDataRecord record, System.Linq.Expressions.Expression<Func<E, T>> propertySelector)
      {
        System.Linq.Expressions.MemberExpression memberExpression = propertySelector.Body as System.Linq.Expressions.MemberExpression;
        System.Reflection.MemberInfo propertyInfo = memberExpression.Member;

        int fieldIndex = record.GetOrdinal(propertyInfo.Name);
        return Field<T>(record, fieldIndex);
      }

      /// <summary>
      /// Retrieves a fields value from an IDataRecord in a strongly typed fashion.
      /// </summary>
      /// <remarks>
      /// This code is based on similar functionality in the EFExtensions project; see
      /// http://code.msdn.microsoft.com/EFExtensions
      /// </remarks>
      /// <typeparam name="T">The data type of the field.</typeparam>
      /// <param name="record">The IDataRecord that contains the data</param>
      /// <param name="ordinal">The field's index in the IDataRecord</param>
      /// <returns>The field's value.</returns>
      public static T Field<T>(this IDataRecord record, int ordinal)
      {
        object value = record.IsDBNull(ordinal) ? null : record.GetValue(ordinal);
        return (T)value;
      }
    }

		/// <summary>
   /// Extensión para ordenamiento por nombre del campo
   /// </summary>
   public static class OrderExtension
   {
		/// <sumary>
		/// Ordena usando una expresión de ordenamiento, por ejemplo:
		///		iqueryable.OrdenarUsandoExpresion("Employee.LastName, Freight DESC")
		/// </sumary>
      public static IOrderedQueryable<TEntity> OrdenarUsandoExpresion<TEntity>(this IQueryable<TEntity> source, string sortExpression) where TEntity : class
      {
             String[] orderFields = sortExpression.Split(',');
             IOrderedQueryable<TEntity> result = null;
              bool sortDescending=false;
              if (sortExpression.Contains(" DESC"))
              {
                  sortDescending = true;
              }
             for (int currentFieldIndex = 0; currentFieldIndex < orderFields.Length; currentFieldIndex++)
             {
                String[] expressionPart = orderFields[currentFieldIndex].Trim().Split(' ');
                String sortField = expressionPart[0];
                //Boolean sortDescending = (expressionPart.Length == 2) && (expressionPart[1].Equals("DESC", StringComparison.OrdinalIgnoreCase));
                if (sortDescending)
                {
                   result = currentFieldIndex == 0 ? source.OrdenarPorDescendente(sortField) : result.EntoncesPorDescendente(sortField);
                }
                else
                {
                   result = currentFieldIndex == 0 ? source.OrdenarPor(sortField) : result.EntoncesPor(sortField);
                }
             }
             return result;
      }

      public static IOrderedQueryable<TEntity> OrdenarPor<TEntity>(this IQueryable<TEntity> source, string fieldName) where TEntity : class
      {
         MethodCallExpression resultExp = GenerateMethodCall<TEntity>(source, "OrderBy", fieldName);
         return source.Provider.CreateQuery<TEntity>(resultExp) as IOrderedQueryable<TEntity>;
      }

      public static IOrderedQueryable<TEntity> OrdenarPorDescendente<TEntity>(this IQueryable<TEntity> source, string fieldName) where TEntity : class
      {
         MethodCallExpression resultExp = GenerateMethodCall<TEntity>(source, "OrderByDescending", fieldName);
         return source.Provider.CreateQuery<TEntity>(resultExp) as IOrderedQueryable<TEntity>;
      }

      public static IOrderedQueryable<TEntity> EntoncesPor<TEntity>(this IOrderedQueryable<TEntity> source, string fieldName) where TEntity : class
      {
         MethodCallExpression resultExp = GenerateMethodCall<TEntity>(source, "ThenBy", fieldName);
         return source.Provider.CreateQuery<TEntity>(resultExp) as IOrderedQueryable<TEntity>;
      }

      public static IOrderedQueryable<TEntity> EntoncesPorDescendente<TEntity>(this IOrderedQueryable<TEntity> source, string fieldName) where TEntity : class
      {
         MethodCallExpression resultExp = GenerateMethodCall<TEntity>(source, "ThenByDescending", fieldName);
         return source.Provider.CreateQuery<TEntity>(resultExp) as IOrderedQueryable<TEntity>;
      }

      private static MethodCallExpression GenerateMethodCall<TEntity>(IQueryable<TEntity> source, string methodName, String fieldName) where TEntity : class
      {
         Type type = typeof(TEntity);
         Type selectorResultType;
         LambdaExpression selector = GenerateSelector<TEntity>(fieldName, out selectorResultType);
         MethodCallExpression resultExp = Expression.Call(typeof(Queryable), methodName,
                         new Type[] { type, selectorResultType },
                         source.Expression, Expression.Quote(selector));
         return resultExp;
      }

      private static LambdaExpression GenerateSelector<TEntity>(String propertyName, out Type resultType) where TEntity : class
      {
         // Create a parameter to pass into the Lambda expression (Entity => Entity.OrderByField).
         var parameter = Expression.Parameter(typeof(TEntity), "Entity");
         //  create the selector part, but support child properties
         PropertyInfo property;
         Expression propertyAccess;
         if (propertyName.Contains('.'))
         {
            // support to be sorted on child fields.
            String[] childProperties = propertyName.Split('.');
            property = typeof(TEntity).GetProperty(childProperties[0], BindingFlags.Instance | BindingFlags.NonPublic | BindingFlags.Public);
            propertyAccess = Expression.MakeMemberAccess(parameter, property);
            for (int i = 1; i < childProperties.Length; i++)
            {
               property = property.PropertyType.GetProperty(childProperties[i], BindingFlags.Instance | BindingFlags.NonPublic | BindingFlags.Public);
               propertyAccess = Expression.MakeMemberAccess(propertyAccess, property);
            }
         }
         else
         {
            property = typeof(TEntity).GetProperty(propertyName, BindingFlags.Instance | BindingFlags.NonPublic | BindingFlags.Public);
            propertyAccess = Expression.MakeMemberAccess(parameter, property);
         }
         resultType = property.PropertyType;
         // Create the order by expression.
         return Expression.Lambda(propertyAccess, parameter);
      }
   }
}

